# BE A PRO, סטטוס הפרויקט

מסמך המשכיות. לפתוח שיחה חדשה, להצמיד את זה בהודעה הראשונה, ולהמשיך.

---

## ⭐ עדכון הסשן האחרון (הכי חשוב, קרא את זה קודם)

### 🎓 קריירת המאמן: פרופיל, נתונים ורישיונות (בוצע ואומת, בקשת איציק)

**הבחירה:** איציק ביקש **השפעה מלאה, כולל על המגרש** (מתוך 3 אופציות).

**1. פרופיל פתיחה (5 ארכיטיפים)** — `src/data/managers.ts` נכתב מחדש. הישן היה `calm/fighter/accountant/dreamer` עם **3 מודיפיירים מתים** (`derbyMorale`/`youthGrowth`/`cardBias` הוגדרו ולא היו בשימוש), והאונבורדינג **קיבע `type:'calm'` ומעולם לא נתן לבחור**. עכשיו: **מפתח הנוער · הצייד · המנטליסט · הכריזמטי · הטקטיקן**, כל אחד עם **56 נקודות בדיוק** (סגנון, לא כוח).

**2. נתונים בשתי קבוצות** (סולם 1-20, שונה בכוונה מ-40-99 של שחקנים):
- **אימון:** התקפה · הגנה · טקטיקה · כושר גופני
- **מנטלי:** נחישות · מוטיבציה · רמת משמעת

**3. רישיונות** (`src/game/coach.ts`): **חובבן → מקצועי (חובה בליגה א׳, ₪120K) → פרו (חובה בליגת העל, ₪900K) → בינלאומי פרו (לאירופה, ₪3.5M)**. השער נכנס ל-`preseasonBlockedReason` הקיים — **בלי הרישיון אי אפשר לפתוח עונה**. הקורס נלמד רק בפרה-סיזן, עולה כסף, ומעלה נתונים לפי מה שהמאמן כבר חזק בו.

**4. כל האפקטים מחוברים, אין קוד מת:**
| נתון | מה הוא עושה בפועל |
|---|---|
| טקטיקה | `chemistry` של הקבוצה (היה מקובע 0.7) |
| התקפה/הגנה | מכפיל `att`/`def` ב-`teamRatings` (שדה `coach` אופציונלי חדש ב-`TeamInput`, ברירת מחדל 1.0) |
| כושר גופני | כושר יציאה מהפרה-סיזן (`ageSquad`) |
| מוטיבציה+נחישות | דריפט מורל אחרי כל משחק |
| מוטיבציה | `youthGrowth` — כמה צעירים מממשים פוטנציאל |
| משמעת | `cardBias` בכרטיסים (`liveMatch`) |

**⚠️ שני באגים קריטיים שנתפסו בסימולציה:**
1. **האפקטים מרוכזים על 10, אבל ארכיטיפ מתחיל בממוצע 8** — כלומר *כל* מאמן התחיל מתחת לבייסליין (att/def רק פגעו, מורל ‑2.9/משחק). **תוקן:** `NEUTRAL = 8`, ועכשיו מאמן פתיחה ≈ ניטרלי והקורסים הם מה שמשפר.
2. **`liveMatch.ts` יש לו `sideRatings` משלו עם `chemistry: 0.7` מקובע** — כלומר אפקטי המאמן **לא היו משפיעים על המשחק שאתה צופה בו**, רק על משחקי AI. **תוקן:** `Side.coach` + `createLive({coach})` + `liveMatchInput` מעביר.

**כיול סופי (400 משחקים, אותם סגלים):** טקטיקן **49%** ניצחונות · ניטרלי **42%** · כריזמטי **41%**. פער 8 נקודות סביב הבייסליין — משמעותי בלי לשבור את הטיפוס. הכריזמטי מחזיר את זה במורל (+2.0/משחק) ובנוער (×1.21) שמצטברים לאורך קריירה. ⚠️ **אל תשנה את `NEUTRAL` או את מדרון ה-chemistry (0.016) בלי להריץ מחדש סימולציית win-rate.**

**זרימה ו-UI:** אונבורדינג עכשיו **6 שלבים** (`הרזומה → מי אתה → המועדון → החתימה → הסגל → השוק`), פאזה חדשה `onboard-archetype`. מסך `Archetype.tsx` מציג את 5 הפרופילים עם שתי קבוצות הנתונים. מסך `Coach.tsx` חדש (מה-Hub, "המאמן שלי") מציג דירוג, נתונים, הסבר מה כל נתון עושה, וסולם הרישיונות עם מה חוסם. `CourseCard` בלוח הפרה-סיזן באדום כשחסר רישיון. `save.ts` מבצע backfill (`coach`) לסייבים ישנים.

**אומת:** headless (ארכיטיפים=56, שערי רישיון, מעבר קורסים, win-rate, שהמאמן מגיע ל-`liveMatchInput` ושונה בין ארכיטיפים) + בדפדפן (5 הפרופילים, שתי קבוצות הנתונים, מעבר בעלים→מאמן, stepper 6 שלבים, 0 שגיאות). tsc + build עוברים.

**נשאר למאמן** (fast-follow): אירופה לא קיימת עדיין במשחק, אז הרישיון הבינלאומי הוא שאיפתי (מוגדר ומוצג, השער יופעל כשאירופה תיבנה) · נתוני המאמן לא עולים מניסיון/עונות, רק מקורסים.

### הסשן הזה (המשך 3): 💰 כלכלה ריאליסטית — שכר לפי דרג, תקציב נמוך, ג' בלי חוזים (בוצע ואומת, בקשת איציק)

איציק: התקציב והחוזים גבוהים מדי לפתיחה; ליגה ג' צריכה להיות אמאטור/בחינם. שלוש דרישות, כולן בוצעו:
1. **תקציב פתיחה נמוך:** `START_MONEY` 400K→**75K** (מיוצא עכשיו, ה-preview באונבורדינג השתמש ב-400K hardcoded — תוקן ל-`G.START_MONEY`). ראש העין מתחיל ב-₪89K.
2. **אין חוזים בעונה 1:** `enterPreseason` ה-firstEver מזריע חוזים מלאים 1-3 (במקום `seedContract-1`=0-2), אז אף אחד לא נגמר בעונה 1. `preseasonEvents` — כרטיסי renew ו-young(העלאה) חסומים ל-`season>=2`. אומת: אפס כרטיסי חוזה ב-3 מחזורי הפרה-סיזן הראשון.
3. **שכר שבועי לפי דרג** (`playerWage(p, tier)` ב-career.ts, בנד לפי דרג × מיקום השחקן בליגה): **ג'/ב' ≤5K · א' 7-12K · לאומית 12-15K · על 25-100K**. השכר עכשiש **שבועי** ומנוכה מלא בכל מחזור (`roundCosts` בלי `/rounds`). `report.wages`+SeasonEnd עודכנו (שבועי).

**דמי העברה לפי דרג** (`transferFee(p, tier)`, `FEE_SCALE` ג'0.05→על1.0): ג' ~₪2-6K (בחינם/אמאטור), מטפס. `sellPrice`/`contractTerms`/`renewTerms` קיבלו tier. ה-tier מושחל בכל הקוראים (state/Transfers.tsx).

**⚠️ כיול קריטי:** עם השכר הנמוך, ה-purse/פרסים הישנים הפכו כל דרג לרווחי (הבעיה ש-HANDOFF נלחם בה חוזרת). **כוילו מחדש למטה** (`seasonPurse` base [90K,240K,700K,1.8M,4.5M], `matchPrize` W45K/D16K/L4K, `PITCH_UPKEEP`/`SECURITY_HOME` הוזלו לג'). אומת headless: ג' אלוף +150K / אמצע ‑22K / **תחתית ‑189K (הפסד)** — המתח חזר. א' תחתית ‑186K. הליגות הגבוהות רווחיות (הכנסות שער אמיתיות מאצטדיון גדול, ריאלי). **אל תיגע ב-purse/prize/wage בלי להריץ סימולציית תזרים מחדש.**

**אומת בשטח:** קריירת ראש העין חדשה — תקציב ₪89K, שוק ₪2-6K, הצעת חוזה שכר ₪3K/שבוע, אפס כרטיסי חוזה בעונה 1, 0 שגיאות. tsc + build עוברים.

---

### הסשן הזה (המשך 2): 🏙️ בחירת מועדון לפי העיר של המשתמש — ליגה גאוגרפית אמיתית (בוצע ואומת)

**הרעיון של איציק:** במקום לבחור מרשימת מועדונים בדיוניים (נחל עוז וכו'), המשתמש **רושם את העיר שלו** והמועדון של העיר נבחר אוטומטית. ראש העין → מ.ס ראש העין, ב"ש → צעירי ב"ש, ת"א → מ.ס ת"א. **המטרה: שייכות.** בחר **ליגה גאוגרפית מלאה** (מתוך 2 אופציות).

**הבחירה החכמה של איציק:** לא הקבוצה המפורסמת של העיר אלא **הקבוצה השנייה, האנדרדוג** — מתאים בול לפתיחה בליגה ג' ומוריד סיכון משפטי.

**מה נבנה:**
- **`src/data/cities.ts`** — 91 ערים ישראליות עם קואורדינטות + `size` (1-3). `searchCities()` השלמה אוטומטית (prefix-first). `clubFromCity()` מחולל מועדון: **~33 ערים curated** (תחילית+צבעים+סמל ביד, כולל כל הדוגמאות של איציק) + **fallback דטרמיניסטי מ-hash** לכל השאר. `buildRegionLeague(city)` = **העיר שלך + 7 הערים הקרובות** = ליגה ג' של האזור שלך, כשה**דרבי = העיר הכי קרובה גאוגרפית**.
- **דרבי דינמי:** הוספתי `rivalId` ל-`Club` ורג'יסטרי מודולרי ל-`isDerby` (`setDerbies`/`derbiesFromClubs`), מסונכרן ב-`pickCity`/`startNextSeason`/`loadCareer` (module state לא שורד reload). לא שבר אף קורא קיים של `isDerby`.
- **`pickCity()` ב-state.ts** — בונה את הליגה האזורית, מסנכרן דרבי, ומאציל ל-`pickClub`. הזהות נישאת קדימה בעלייה (buildNextSeason שומר myClub), אז מספיק שליגה ג' גאוגרפית; הליגות הגבוהות נשארות בדיוניות (fast-follow אם רוצים).
- **מסך אונבורדינג שוכתב** (`Onboard.tsx` → `OnboardClub`): שדה חיפוש עם dropdown השלמה, ואז **רגע חשיפה** — הסמל נבנה בצבעי העיר, שם המועדון, "הדרבי שלך: X", וצ'יפים של כל ערי האזור. `App.tsx` קורא ל-`pickCity(city)`.

**⚠️ באג שנתפס באימות:** `hash >> 3` הוא shift חתום, וכשה-hash גדול מ-2^31 יצא שלילי → `negative % length` = index שלילי = `undefined`. **תוקן** ל-`>>>` (unsigned) בכל המחלץ.

**אומת:** headless (91 ערים, כל אזור מתמלא בערים אמיתיות, דרבי דו-כיווני עובד, 8 ids ייחודיים, יציבות). בדפדפן: אונבורדינג מלא ("ראש" → ראשון/ראש העין/ראש פינה), חשיפה נכונה, אישור → signing עם "מ.ס ראש העין", והסייב מכיל ליגה גאוגרפית מלאה (8 ערי מרכז, rival=הפועל כפר קאסם, 56 מחזורים). tsc + build עוברים.

**✅ שם העיר ארוג באוהדים ובעיתונות** (בוצע ואומת): הוספתי `city` ל-`FanContext` ול-`PressContext` (נבנה מ-`club(gs).city`). ב-`fans.ts`: 5 שורות לפני משחק (hope/heart/sage/nostalgic/numbers) + 3 אחרי (ניצחון/הפסד) שאומרות את שם העיר ("כל ראש העין מדברת רק על המשחק", "הלילה כל ראש העין חוגגת"). ב-`press.ts`: מחולל `LOCAL` עם 3 שאלות מזווית מקומית + **outlet "מקומון {עיר}"** שנבחר ~25% מהזמן. אומת headless: ~27% משורות האוהד מזכירות את העיר, מקומון ב-25% מהערכים, הכל בקולות הנכונים.

**✅ ליגות גבוהות גאוגרפיות לפי גודל עיר** (בוצע ואומת, בקשת איציק): כל הדרגים 2-5 עכשיו ערים אמיתיות ולא בדיוניות. `cityClubsForTier(tier)` ב-cities.ts עם `TIER_ROSTER`: **על=ענקיות** (ת"א/חיפה/ירושלים/ב"ש/אשדוד/ראשל"צ/פ"ת/נתניה/חולון/ב"ב/ר"ג), **לאומית=גדולות+כמה בינוניות** (אשקלון/רחובות/חדרה/מודיעין/לוד/קריית גת...), **א'=בינוניות**, **ב'=עיירות**. `poolForTier` ב-career.ts קורא לזה במקום `clubsForTier` הבדיוני (leagues.ts כבר לא בשימוש ל-clubs, נשאר ל-initLeague/fixtures). **המסלול המלא אומת headless:** מכבי שעריים (רחובות) עלה מלאומית→על ומצא את עצמו מול מ.ס ת"א, מ.ס חיפה, מכבי ירושלים, צעירי ב"ש — המועדון נשמר, ids ייחודיים. הפנטזיה: מקבוצת שכונה בליגה ג' עד להתמודד מול הענקיות.

**נשאר לעיר** (fast-follow): (1) להרחיב מ-91 ל-1,200 יישובים מהטאב של איציק. (2) המאמן "חוזר לעיר שלו". (3) שם העיר בכרוניקה/וואטסאפ. (4) רלגציה לג' חוזרת ל-LEAGUE_C הבדיוני (poolForTier(1)) במקום לאזור — edge case נדיר.

---

### הסשן הזה (המשך 1): 🎴 קלפי שחקנים לפי נדירות + מונטיזציית יהלומים (בוצע ואומת)

**עיצוב הקלף, "לוח שידור".** איציק פסל את הבסיס מהסקיל (`ultra-card.html`) כ"מזכיר מדי את FIFA". הוצגו 3 כיוונים חלופיים (לוח שידור / חולצת גב / באנר יציע), נבחר **לוח שידור**: גוף כהה של לילה־אצטדיון **קבוע בכל הדרגים**, והנדירות צובעת רק **מבטאים** (פס־קצה, צבע ה-OVR, שלט־שם מוטה, ברי סטטיסטיקה) + חיתוך ה-chamfer של המשחק. **בלי טקסט שם-דרג על הקלף** (בקשת איציק). `src/ui/components/UltraCard.tsx`, 4 גדלים (xl/l/m/s) מ-`--w` יחיד, shimmer רק ב-elite ורק ב-xl.

**רמפת צבע נבדלת** (איציק ביקש "צבעים ממש שונים אחד מהשני", ואז "הרגיל והכסף דומים, תעשה את הרגיל ממש אפור כהה"):
גרפיט כהה `#454b53` (רגיל) → חום `#9a5f2e` → נחושת `#ef7d33` → **כסף קר** `#bcd0e6` → זהב `#e3b431` → זהב זוהר `#ffd662`. הכסף הקר באמצע שובר את הרצף החם.

**מיפוי רייטינג→דרג, סקאלה אבסולוטית** (`src/game/cards.ts`). **החלטה: אבסולוטי, לא יחסי לליגה** — כדי שדרג הקלף יסמן איכות אמיתית ניידת, אחרת פרס הפרסומת חסר ערך. ⚠️ **המיפוי בסקיל (`card-system.md`) הוא של FIFA ולא מתאים למשחק** (ברונזה<65 היה עושה את כל ליגה ג' וב' לדרג אחד). מכויל ל-`leagueCeiling = 47.5 + 5.5·tier`:
`≤56 רגיל · 57-62 חום · 63-68 ארד · 69-75 כסף · 76-83 זהב · 84+ זוהר`
**אומת headless** (60 סגלים לדרג): ג׳ **79% רגיל / 21% חום** (בדיוק "כמעט כולם רגילים + בודדים"), ב׳ חום, א׳ ארד, לאומית כסף, על כסף/זהב. נבחר מתוך 3 סטי ספים.
גם: `ovrColor()` אוחד לכל המשחק (Squad/Transfers מייבאים מ-cards.ts, לא כפילות).

**מטבע יהלומים + חבילות** (`src/game/packs.ts`). **הכלכלה שאיציק הגדיר:** 3💎 בהתחלה · +1💎 לפרסומת, **מקסימום 3 בעונה** · +5💎 בעלייה. חבילות: **חיזוק 5💎 · כוכב 8💎 · פרו 10💎**.
**חוק הברזל:** `packCeiling(tier) = leagueCeiling(tier+1) - 2` — שום חבילה לא נותנת שחקן מעל **בסיס הליגה שמעליך**. אומת: **0 הפרות** ב-45,000 פתיחות. **elite (84+) לא ניתן להשגה מחבילה בשום דרג** — נשאר שאיפתי.
💎 נפרד לגמרי מהשקלים, אין המרה, אז התקציב לא קונה קלף עולמי.

**שם ואייקון:** נשאר "יהלום", אבל **האימוג'י 💎 נפסל כ"לא גברי"**. נבנה אייקון SVG **פלדה/גאנמטל** (`src/ui/components/Gem.tsx`, נבחר מתוך 3: אובסידיאן/פלדה/ספיר).

**⚠️ שלושה באגים אמיתיים שנתפסו באימות, לא בעין:**
1. **תקרת החבילה לא נאכפה** — הוחלה על ה-base הנומינלי, אבל `attrFor` נותן `hi()` ומחזיר OVR ~5 מעל (הגוצ'ה שכבר מתועדת). 819 הפרות. **תוקן:** לולאת roll מתקנת-עצמה שמודדת OVR אמיתי ומכווננת `bias`.
2. **פרו (10💎) היה גרוע מכוכב (8💎)** — שניהם נצמדו לתקרה, אז אותו רייטינג, אבל כוכב צעיר יותר. איש לא היה קונה פרו. **תוקן:** הסולם מפריד ב**upside** ולא רק ברייטינג (חיזוק/כוכב = צעירים שגדלים, פרו = מוכן עכשיו), + `skew` (פרו נוטה לראש הטווח) + `maxAge`.
3. **תכונות שוער יצאו float** (`56.35608717286959`) — **באג קיים מראש בקוד החי!** `attrFor` מעגל, אבל בלוק ה-GK ב-`squadGen.makePlayer` העביר `lvl` גולמי, ו-`leagueCeiling` הוא `.5` בדרגים זוגיים. כלומר **השוק בליגה ב׳/לאומית כבר מייצר שוערים עם שברים**. תוקן במקור (`makePlayer`), מתקן גם את השוק.

**כוריאוגרפיית פתיחה** (`src/ui/screens/Packs.tsx`, לפי `card-system.md`): חושך → רעד → אור שצבעו רומז לנדירות → הקלף עולה → היפוך → חשיפה. **דילוג בנגיעה**. אחרי: **צרף לסגל** (חוזה 2 עונות) או **מכור** (90% מהערך) — "אין תוצאה ריקה".
**שורת השוואה לסגל** ("השחקן הכי טוב בסגל שלך" / "מעל ממוצע הסגל (55)"): בליגות הנמוכות התקרה כולאת גם את פרו בדרג "רגיל", אז שם הדרג לבדו מוכר בחסר קלף שנכנס ישר להרכב. הדירוג היחסי אומר את האמת שהצבע לא יכול.

**איפה זה מחובר:** כרטיס השחקן (`PlayerCard`) מוביל בקלף XL · Hub עם `PacksTile` · פאזה `packs` ב-App · `gems`/`adsWatched`/`pull` ב-GameState + backfill ב-`save.ts` (סייב ישן מקבל 3💎 ולא קורס, נבדק בפועל) · +5💎 ו-איפוס פרסומות ב-`startNextSeason`.
**אומת סוף-לסוף בדפדפן:** פרסומת 11→12💎 ומונה 3→2 · פרו 30→20💎, סגל 16→17, חוזה 2 שנים · מכירה ₪369K→₪441.9K (+₪72.9K מדויק) · 0 שגיאות קונסול בטאב נקי · tsc נקי · build עובר.
**הערה:** ה-`ReferenceError` בקונסול היה **שאריות HMR** (הגוצ'ה המתועדת), טאב חדש נקי לגמרי.

**מה נשאר לקלפים:** רשימות הסגל/שוק נשארו **שורות** ולא קלפים בכוונה (חוק ה-60fps ב-`card-system.md`), אפשר לשקול קלפי M ברשת · תמונות שחקן אמיתיות (עכשיו צללית) · פרסומת אמיתית (עכשיו הכפתור מזכה מיידית, אין SDK) · כפילויות/מיזוג · "קלף יציע" שבועי.

---

### הסשן שלפניו: השקה + ליטושי UI/UX (המשחק כבר חי ומשוחק בנייד)

**המשחק פרוס וחי, MVP לבדיקה:**
- **אתר:** https://itzik200592-byte.github.io/BE-A-PRO/  ·  **קוד כניסה: 100**
- **Repo ציבורי:** github.com/itzik200592-byte/BE-A-PRO (gh מחובר כ-`itzik200592-byte`, הרשאות repo+workflow).
- **פריסה אוטומטית** ב-GitHub Actions (`.github/workflows/deploy.yml`) בכל `git push` ל-main. פרטים ב-`DEPLOY.md`.
- **base=`/BE-A-PRO/`** ב-`vite.config.ts` (Pages תת-נתיב). **כל נתיבי ה-public עוברים דרך `asset()` ב-`src/ui/asset.ts`.** תמונה חדשה מ-public → חובה `asset('/...')`.
- Pages הודלק ידנית פעם אחת (`gh api repos/.../pages -X POST -f build_type=workflow`), לא צריך שוב.
- **לעדכן:** commit + `git push` (במצב auto הדחיפה דורשת אישור/הרצה על ידי המשתמש). מחיקת שרת LAN/tunnel, לא בשימוש יותר.

**שער כניסה רך** (`src/ui/screens/Gate.tsx`): קוד `100`, נשמר ב-localStorage `beapro.gate`, לפני הפתיח ב-App. למונטיזציה/בדיקה סגורה.

**לוגו חדש** `public/logo.webp` (מאמן חוגג עם אגרוף, מיוצר Higgsfield, מקור `PIC/logo-coach.png`), מחליף את הכדור/B במסך הפתיחה ובשער.

**תיקון גלילה קריטי:** `useLayoutEffect` ב-App לפי `screenKey` מאפס גלילה לראש בכל מעבר מסך (קודם המסך הבא נפתח באמצע ונראה תקוע).

**טיפוגרפיה + נייד:** כל סולם הגדלים הוגדל (tokens.css + inline) לקריאוּת בנייד. סרגל המדדים (`.meters`) בטוח מול notch (`env(safe-area-inset-top)`). metas ל"הוסף למסך הבית" מסך מלא ב-index.html. **הערה: היה באג שהחלפת sed גורפת השטיחה גדלים, שוחזר ידנית סולם מכוון.**

**דף חלון ההעברות שוכתב (הדף הראשון, בקשת איציק):** `PreSeason.tsx` עכשיו כולל **פאנל "הסגל שלך"** מחולק לקווים (שוער/הגנה/קישור/התקפה) עם מונה פר קו, דגל **"צריך תגבור"** על קו דל, תגית **"חסר: ..."/"סגל מאוזן"**, וצ'יפ שחקן שנלחץ פותח כרטיס מלא. באנר "חלון העברות" גדול, סיכום המועדון מקופל לתוכו. **חסימת סיום:** `preseasonBlockedReason` לא נותן לצאת לעונה במחזור האחרון עם חוזים שנגמרו ולא טופלו.

**שוק לפי סינון + כיסוי (תיקן באג אמיתי):** `MARKET_SPINE` ב-`transfers.ts` מבטיח שכל שוק מכסה את כל הקווים עם **2 שוערים לפחות** (size 12). מסך ההעברות קיבל פילטר לפי קו (הכל/שוער/...) עם מונים, ו`marketFocus` ב-state. **"צריך תגבור" פותח את השוק מסונן לעמדה החסרה.** אומת מקצה לקצה (מכר שוער → "צריך תגבור" → שוק מסונן ל-2 שוערים).

**מספרי אצטדיון עודכנו (בקשת איציק):** התחלה **50 מקומות**, הרחבה +100 / יציע חדש +300 בליגה ג'. `stadiumImageTier` band 0-4: <150→0(פתיחה) <1000→1 <5000→2 <15000→3 else 4. `requiredCapacity` {א'/tier3:5000, לאומית/4:10000, על/5:18000}. חץ "אחרי הבנייה" תוקן.

**נקה:** תצוגות ה-dev הזמניות (`?pens ?stadium` וכו') **הוסרו** מ-`App.tsx` ומ-`MomentGallery`, כדי שלא יעקפו את השער.

### 🎴 קלפי שחקנים + מונטיזציה — ✅ בוצע ואומת
ראה "עדכון הסשן האחרון" בראש המסמך.

---


**בניית אצטדיון עם 4 תמונות אמיתיות (יעד 3 הושלם ואומת).** מערכת פיתוח מגרש שגדל עם המועדון:
- **4 תמונות מיוצרות (Higgsfield, לא SVG)** ב-`public/stadium/tier{1..4}.webp` (webp ~115-180KB). איציק ייצר, שיפר בכמה סבבים, והעלה: ג׳ = מגרש שכונתי ישראלי עם בנייני אבן ודודי שמש, ב׳ = אצטדיון קטן עם יציע מרכזי, א׳/לאומית = שני יציעים גדולים עם קצוות פתוחים, על = באולם מודרני מלא. **הקבצים היפים ב-`PIC/איצטדיונים/` (PNG מקור).** ניסיון ראשון היה SVG פרמטרי, איציק פסל ("מעפן"), עברנו לתמונות.
- **תמונת פתיחה (band 0)** בבקשת איציק, **קיימת** ב-`public/stadium/tier0.webp`: אצטדיון פתיחה זעיר, מגרש עפר מקולף שאפשר להיכנס אליו, ספסלי עץ עם מקומיים, גדר חלקית ישנה, שכונה ישראלית עם בנייני אבן ודודי שמש בשקיעה. יש עדיין **fallback ל-tier1** (`onError` ב-`Stadium.tsx`) כרשת ביטחון אם תמונה חסרה. **כל 5 התמונות (tier0-4) במקום.**
- **התמונה נבחרת לפי קיבולת** (`stadiumImageTier`, 0-4): <1000→0 (פתיחה), <2500→1, <7000→2, <20000→3, ≥20000→4. התחלה 900 = band 0, בנייה ראשונה כבר משדרגת את המראה.
- **תצוגת "עכשיו → אחרי הבנייה"** (`BuildThumb`): כל חבילה שחוצה דרגת תמונה מציגה שתי תמונות ממוזערות עם חץ, כדי לראות מה בונים לפני שמשלמים.
- **חשיפת אצטדיון אחרי בנייה** (`StadiumRevealOverlay` ב-`Stadium.tsx`, `stadiumReveal` ב-state): כשבנייה מסתיימת, במסך התוצאה קופצת חשיפה קולנועית מסך-מלא (push-in + כותרת "האצטדיון החדש"/"האצטדיון גדל" + קיבולת), אותה שפה של הפתיח וחגיגת האלופים. נמחק ב-`continueFromResult`.
- **בנייה שלוקחת זמן** (`career.ts`: `expansionOptions`, `state.ts`: `startStadiumProject`/`advanceStadium`): 2 חבילות פר דרגה (הרחבת יציע / יציע חדש), עלות מראש, נבנה על פני 2-4 מחזורים (`roundsLeft` יורד ב-`commitRound`), בסיום הקיבולת גדלה + פרק כרוניקה "האצטדיון גדל". פרויקט אחד בכל פעם.
- **הכנסות שער** (`gateIncome`, `attendanceFill`): כל משחק בית, קיבולת × תפוסה × מחיר כרטיס לפי דרג, נוסף כשורה במאזן במסך התוצאה (`Result.tsx`). תפוסה מיוקרה+פורם+דרבי. אצטדיון גדול = החזר גדול.
- **חובה מליגה א׳ = חוסם התקדמות קשוח** (בחירת איציק): `requiredCapacity` = {א׳:7000, לאומית:12000, על:20000}. `buildNextSeason` מקבל `stadiumOk`, ואם עלייה נרשמה על המגרש אבל הקיבולת מתחת לדרישה, **העלייה נחסמת** (אפילו אלוף נשאר). `SeasonEnd` מציג "האצטדיון קטן מדי, נשארים", ה-Hub מראה באדג׳ אדום, מסך האצטדיון מראה כמה מקומות חסרים.
- קבצים: `game/career.ts` (כלכלת אצטדיון + `requiredCapacity`/`stadiumImageTier`/`gateIncome`/`expansionOptions`/`fillWithYouth` כבר קיים), `game/state.ts` (`stadium` ב-GameState, `openStadium`/`startStadiumProject`/`stadiumGate`/`homeGateEstimate`), `ui/screens/Stadium.tsx` (המסך), כרטיס ב-`Hub.tsx`, שורת שער ב-`Result.tsx`, חסימה ב-`SeasonEnd.tsx`. אומת headless (17 asserts) + פליי-תרו מלא (4 תמונות נטענות ומתחלפות, בנייה מהמסך מורידה כסף ומראה פרויקט).
- **תצוגת dev זמנית למחיקה:** ראוט `?stadium=1..4` (מראה כל דרגה) ב-`App.tsx`.

**פרה-סיזן אמיתי של 3 מחזורים (יעד 1 הושלם ואומת).** קודם קפצו ישר למחזור 1. עכשיו יש קיץ אמיתי: שלושה מחזורי העברות בלי משחק, ורק אחריו הליגה מתחילה. השוק פתוח בכל שלושת המחזורים (בחירת איציק), וההודעות מתגלגלות תוך כדי:
- **מחזור 1, בקשות עזיבה** (`preseasonEvents` ב-`state.ts`, מחולל ב-`preseason.ts`): הכוכב הכי טוב (שחקן שדה עד גיל 30) "רוצה לעלות ליגה" → למכור בדמי העברה ×1.7 (מורל +2, הסגל נחלש) או לחסום (מורל -5). הצעיר הכי טוב (עד 21) "רוצה חוזה חדש" → העלאה (בונוס נאמנות, חוזה ל-3, מורל +4) או סירוב (מורל -4, נשאר על חוזה שנה). מגלגל את השחקנים האמיתיים מהסגל.
- **מחזור 2, חוזים שנגמרו**: כל מי שהחוזה שלו ב-0 → חידוש (`renewTerms`, דמי חתימה מוזלים) או שחרור חופשי. **dedup**: מי שנמצא בסאגת עזיבה לא מופיע גם כחידוש, שחקן לא ממלא שני כרטיסים.
- **מחזור 3, קריאה אחרונה**: מי שהחוזה נגמר ולא חודש עד השריקה, הולך חופשי (אבל אף פעם לא מתחת ל-16, אז אם קצר מוסיפים נוער דרך `fillWithYouth` ב-`career.ts`).

**מערכת חוזים חדשה (בלי לגעת במנוע):** `contracts: Record<playerId, number>` ב-GameState, הסגל שלי בלבד. בלי מיגרציה, חוזה חסר נגזר 1-3 שנים מ-hash (`seedContract`). כל פרה-סיזן יורדת שנה, מי שב-0 נכנס לחידושים. חתימה בשוק קובעת חוזה מ-`contractTerms.years`. נוער שעולה מהאקדמיה מקבל 3 שנים. `save.ts` ממלא את השדות החדשים (`contracts`/`preWeek`/`preResolved`) לסייבים ישנים.

**זרימה חדשה:** קריירה חדשה `squad → enterPreseason (phase 'preseason-market', preWeek 1..3) → finishPreseason → enterSeason (hub, מחזור 1)`. עונה 2+ `season-end → startNextSeason (phase 'preseason', מסך הקיץ הקיים) → enterPreseason → ...`. **חלון הקיץ בתוך העונה נסגר** (`windowState` עכשיו רק חורף 8-9), כל פעילות הקיץ בפרה-סיזן. קבצים חדשים: `game/preseason.ts`, `ui/screens/PreSeason.tsx` (הלוח, `PreSeasonMarket`). **`MarketIntro.tsx` נמחק** (`market-intro` הוסר מ-Phase). אומת headless (שני שערי כניסה) + פליי-תרו מלא בדפדפן.

**מאגר אוהדים גדול עם אישיויות (יעד 2 הושלם ואומת).** קודם: 4 אוהדים, ~14 שורות, רובן מצחיקות, חוזרות מהר. עכשיו `data/fans.ts` שוכתב למערכת voice מלאה:
- **11 אוהדים, כל אחד עם `voice`**: `sage` (היודע-כל, מבוגר שבטוח שהוא מבין יותר מהמאמן, בקשת איציק, 3 אוהדים), `doom` (נביא זעם), `nostalgic` (בזמני), `hope` (אופטימיסט נצחי), `super` (אמונות טפלות), `hot` (לחץ דם), `numbers` (מצטט טבלה), `heart` (לוקח ללב).
- **התאמת voice לשורה**: שורות טקטיות של "היודע-כל" נאמרות רק על ידי אוהד sage, וכו'. שורות ה-sage **מגיבות לטקטיקה שלך בפועל** (`ctx.approach`): "אתה נפתח יותר מדי" / "למה להסתגר". היודע-כל תמיד eligible לפני משחק, אז הוא נוכחות קבועה. אחרי ניצחון לוקח קרדיט ("סוף סוף הקשבת לי"), אחרי הפסד "אמרתי לך".
- **סלוטים** (מערך + picker לפי seed): פורמציות, מקצועות, שמות ותיקים, אז אותה שורה מתנסחת אחרת.
- **אנטי-חזרתיות**: `fanHistory` ב-GameState (cap 16), נרשם ב-`startWeek` (pre) וב-`endOfWeek` (post), מועבר כ-`recent` ל-`fanMessage` שמעדיף שורות שלא נשמעו לאחרונה. `fanNote` נשאר טהור לרינדור (דטרמיניסטי לפי seed שבוע).
- **אימות headless**: 45 שורות שונות ב-4500 הגרלות על פני 15 הקשרים, 100% התאמת voice, sage נוכח לפני כל משחק, 8 אישיויות, אנטי-חזרתיות עובדת. פליי-תרו: FanNote מרנדר "דוד הנביא" עם שורת doom, `startWeek` רץ נקי.

**אימות headless מהיר:** Node 24 מריץ `.ts` ישירות עם `node --experimental-strip-types file.mts` (הפרויקט מייבא עם סיומת `.ts` מפורשת, זה בדיוק מה שstripping צריך). שימושי לבדיקת לוגיקת state בלי DOM.

---

### הסשן שלפניו (עדיין רלוונטי)

הסשן היה ענק. נבנו הרבה מערכות חדשות, הכל עובר tsc נקי ואומת. סיכום:

**רגעי משחק ויזואליים (הוחלף מהתמונות הסטטיות הישנות):** לכל רגע החלטה יש עכשיו **סט תמונות POV/שידור** (בילדאפ + תמונת תוצאה לפי הבחירה). נוצרו ב-Higgsfield, דחוסות ל-webp ב-`public/moments/<kind>/`. הסטים: `penalty` (7), `free-kick` (8, כולל חומה), `shot` (9, כולל החמצות), `one-on-one` (5), `def-keeper` (5), `def-tackle` (7). כולם עם חולצה שחורה/מכנס לבן לבועט, שוער אפור, יריב לבן, בלי לוגו. מחוברים ב-`Match.tsx` (MomentPopup + כרטיסי outcome פר סוג). המנוע (`liveMatch.ts`) מחזיר את סוג התוצאה מכל `resolve*`.

**תמונות מאמן מהורהר לרגע הטקטיקה בפיגור** ב-`public/moments/tactic/tier{1,2,3}.webp`, מדורג לפי דרג (ג'-לאומית / על / אירופה), מאמן מגבו בלי פנים. נבחר לפי `club.tier` ב-`tacticImg()`.

**פתיח קולנועי** (`src/ui/screens/Intro.tsx`), 4 סצנות (ספסל → מאמן מתחיל → אצטדיון בינוני → חליפה באצטדיון ענק, `public/intro/scene{1-4}.webp`), Ken Burns + הבזק זרקורים + לטרבוקס + כותרת. **רץ בכל פתיחת משחק**, דילוג בטאפ.

**מגרש 2D חי** במרכז עמוד המשחק, החליף את פיד הטקסט. **לולאת rAF ב-60fps** שכותבת ישירות ל-DOM. מתחתיו **טיקר** קליל (2 שורות פרשנות). **BenchBar** חדש לחילופים, שחקן מתעייף צף עם כפתור חילוף מהיר בנגיעה אחת.

⭐ **הכדורגל עצמו יושב ב-`src/game/pitchSim.ts`, לא ברכיב.** `LivePitch.tsx` הוא רק חיווט: בונה סלוטים לפי המערך, מזין אירועי פרשנות, וכותב מיקומים ל-DOM. הסיבה: **הפאנל של קלוד לא מרנדר, `requestAnimationFrame` לא מתקתק שם, ואי אפשר לראות את המגרש**. שלוש פעמים שוכתב על ניחוש. עכשיו:

```bash
npm run check:pitch        # 36 ריצות × 90 דקות מסומלצות, מדווח מה באמת קרה
npm run check:formations   # 4000 משחקים לכל צימוד מערכים, אותם סגלים
node --experimental-strip-types scripts/pitch-snap.mts out.svg 4-4-2 4-3-3   # 6 תמונות מהמגרש
```

**מידות אצטדיון אמיתיות + שילוט (31.8, מספרים של איציק).** האצטדיונים היו נוסחה (`small`, `big = small*3`) ולא כדורגל ישראלי. עכשיו מפורשים:

| ליגה | קהל | הרחבת יציע | יציע חדש | עלות/מקום | החזר |
|---|---|---|---|---|---|
| ג' | 250 | +100 (₪40K) | +250 (₪108K) | ₪400 | 4.0-4.3 |
| ב' | 700 | +500 (₪300K) | +750 (₪486K) | ₪600 | 4.0-4.3 |
| א' | 1,500 | +1,000 (₪900K) | +1,500 (₪1.46M) | ₪900 | 4.0-4.3 |
| לאומית | 4,500 | +2,500 (₪3.38M) | +5,000 (₪7.29M) | ₪1,350 | 4.0-4.3 |
| על | 16,000 | +12,000 (₪24.6M) | +30,000 (₪66.4M) | ₪2,050 | 4.0-4.3 |

⚠️ **נגזרת חובה: דרישות העלייה שונו.** `TIER_MIN` `{3:5000, 4:10000, 5:18000}` → **`{3:1200, 4:3500, 5:12000}`**. אם ליגה א' מביאה 1,500 איש, לדרוש 5,000 מקומות כדי להיכנס אליה זו דרישה לאצטדיון פי שלושה מהקהל של הליגה עצמה. הסף עכשיו מעט מתחת לביקוש של ליגת היעד.

**שילוט באצטדיון** (`signageRound`, `SIGN_RATE=[2.5,3.5,5,7,10]` למקום למחזור): הספונסר משלם על **מקומות ולא על קהל**, כי הפרסום נקנה לאצטדיון ולא למי שהגיע השבוע. זה נותן ליציע **דרך שנייה להחזיר את עצמו**, וזו הסיבה היחידה לבנות מעט לפני הביקוש. שורה נפרדת ב-ledger ובמסך האצטדיון.

⚠️ `gateIncome` חתימה: `(attendance, tier)`. `EXPECTED_CROWD` ב-sponsor.ts **חייב להישאר מסונכרן** עם `DEMAND` ב-career.ts.

⚠️ **החסות חוסנה את המועדון וכמעט הרגה את הפיטורים.** אחרי השילוט, **העונה הגרועה ביותר בכל ליגה יצאה רווחית** — כלומר אי אפשר להיות מפוטר בכלל. `SEASON_VALUE` הוקטן לחצי: `[45k, 85k, 280k, 575k, 1.35M]`. עכשיו עונה גרועה מפסידה כסף בליגות ג'/א'/לאומית, והאצטדיון עדיין בר-מימון. **אחרי כל שינוי בחסות או בשילוט חובה להריץ `check:debt` ו-`check:climb`, לא רק `check:economy`.**

📌 **מסקנה מכוונת: בליגה ג' אי אפשר להיות מפוטר.** התקציב השנתי כולו (~300k) קטן מהחבל שהבעלים נותן (150k), ואין שם על מה לבזבז — 6 עונות של בזבוז הכל על יציעים מסתיימות ב-₪1,978 מינוס. זה נכון: ליגה ג' היא הליגה שבה לומדים. **הנשיכה היא בקפיצת השכר פי 6 לליגה א'** — שם קבוצה שעלתה ולא יכולה לשאת את הליגה מפוטרת. יש בדיקה לשני הצדדים.

⭐⭐⭐ **האצטדיון וחסות ULTRASKIT (31.8).** האצטדיון היה **הפוך**: החזר השקעה של **14 עונות בליגה ג' מול עונה אחת בליגת העל**. השקעה נוראית כשאתה עני, מכונת כסף כשאתה עשיר.

**1. מחיר למקום** `[900,500,320,300,380]` → **`[230,390,660,1100,1850]`**. החזר עכשיו **3.6-5.4 עונות בכל הליגות** — החלטה אמיתית בכל מקום. ההרחבה הראשונה בליגה ג' ירדה מ-₪90K ל-**₪23K**, כלומר בת-השגה.

**2. ⭐ ביקוש קהל — התיקון הכי חשוב כאן.** `attendanceFill` החזיר אחוז **מהקיבולת**, כלומר **אצטדיון גדול יותר תמיד הביא יותר אוהדים**. מדידה: קריירה סיימה **בליגה ג' עם 21,363 מקומות ו-₪4.16 מיליון**. עכשיו `crowdDemand(tier, prestige)` (`DEMAND=[400,1800,7000,14000,26000]`) קובע כמה אנשים העיר בכלל מוציאה, ו-`homeAttendance = min(קיבולת, ביקוש) × תפוסה`. **מקומות מעבר לביקוש לא מכניסים כלום.**
- `gateIncome` שינה חתימה: `(attendance, tier)` במקום `(capacity, fill, tier)`.
- מסך האצטדיון מציג עכשיו **"כמה קהל הליגה מביאה"** ומשפט שאומר אם הרחבה היא השקעה או קישוט. **בלי המידע הזה השחקן לא יכול להחליט.**

**3. חסות** (`src/game/sponsor.ts`): `ULTRASKIT` — **placeholder, יוחלף בפרסומות אמיתיות**. שלוש עסקאות שנבחרות בכל קיץ (`phase: 'sponsor'`, נכפה ב-`enterSeason`): **בסיס** (קבוע), **הישגים** (60% + מענק ×1.35 על עלייה), **יציע** (55% × 0.35-2.0 לפי הקהל בפועל). משולם בכל מחזור דרך ה-ledger.

⚠️ **עוד שתי רצפות שמחקו חוב נמצאו והוסרו**: `money: Math.max(0, rawMoney)` ב-`startNextSeason` (חוב התאדה כל קיץ), ושלוש `Math.max(0, ...)` על רכישות.

```bash
npm run check:climb     # 8 קריירות × 12 עונות, בונות בזהירות, לכל אחת מ-3 העסקאות
```
**התוצאה:** 0/8 פיטורים · קיבולת מסתיימת 2,900-4,263 (לא 21,000) · **חוזה היציע הוא זה שהכי מקדם**: 4/8 מגיעים ל-5,000 מקומות מול 2/8, ו-2/8 עולים לליגה א' מול 0-1. בדיוק ההבדל שהעסקאות אמורות ליצור.

📌 **פתוח:** רק 1-2 מתוך 8 מגיעים לליגה א' תוך 12 עונות. שער ה-5,000 מקומות + סיום בשתי הראשונות זה מסלול ארוך מאוד. שווה בדיקה אם ההתקדמות בין ליגות איטית מדי בפני עצמה.

⭐⭐⭐ **קשת הפיטורים: אזהרה → הפסד → מכתב → השיחה → הנקמה (2.9).** הפיטורים היו מסך אחד וסוף קריירה. עכשיו זה סיפור.

**1. אזהרה בפנים, פעם אחת בעונה.** `phase: 'ultimatum'` נורה כשהחוב חוצה 75%. הבעלים אומר במפורש: **"ההפסד הבא הוא האחרון שלך"**.

**2. ⭐ הפיטורים דורשים הפסד.** `checkTheBooks(gs, lost)` — מעבר לקו אבל ניצחת? שרדת עוד שבוע. זה הופך חוק בגיליון למשחק שאתה חייב ללכת לנצח בו.

**3. המכתב** (`Sacked.tsx`) עם `public/sacked.webp` — המאמן יוצא מהמשרד עם ארגז (משרוקית, נעליים, קונוסים, לוח טקטיקה), משרד היו"ר מאחוריו.

**4. ⭐ השיחה מהקבוצה השנייה בעיר** (`Rescue.tsx`, `siblingClub`/`buildSiblingLeague` ב-cities.ts). כל עיר ישראלית יש בה שתי קבוצות והן שונאות זו את זו, אז מי שמתקשר זה בדיוק מי שתמיד היה בצל. ליגה מתחת, אותה עיר, prefix וצבעים שונים. **המאמן, הרישיון והכרוניקה עוברים איתו** — הסיפור שלו, לא של המועדון. `takeRescue` בונה מועדון/ליגה/תקציב מאפס.

**5. ⭐ הנקמה.** `nemesis` נשמר ב-state **מוצמד לדרג שבו הם היו**. ביום שאתה מטפס חזרה לליגה שלהם, `returnOfTheNemesis` מחליף את הקבוצה החלשה בליגה בהם (ומעביר את הסגל, כדי שגודל הליגה והלוח לא ישתנו), וכותב `rivalId` הדדי — כלומר **דרבי**.

⚠️ **באג שהבדיקה תפסה:** שכפלתי את לוגיקת העלייה (`result === 'promoted' ? +1`) במקום לקרוא את הדרג בפועל. **אלוף שהעלייה שלו נחסמה בגלל אצטדיון קטן לא עולה**, והיריבה הופיעה בליגה שהמאמן לא הגיע אליה. עכשיו נקרא מ-`next.clubs`.

📌 **מקרה קצה שטופל:** פיטורים בליגה ג' — אין ליגה מתחת, אז הקבוצה השנייה באותה ליגה, היריבה מוכנסת **מיד**, והטקסט מתחלף ("הדרבי כבר העונה", 2 שלבים במקום 3).

⚠️ **השמירה כבר לא נמחקת בפיטורים** — הקריירה ממשיכה דרך ההצלה. היא נמחקת רק ב"לתלות את הנעליים".

```bash
npm run check:sacking   # 38 בדיקות על כל הקשת
```

⭐⭐⭐ **חוב אמיתי ופיטורים (31.8, בקשת איציק).** קודם הכסף היה מוצמד ל-0 (`Math.max(0, ...)`), כלומר **חוב פשוט התאדה**.

**המערכת** (`src/game/finance.ts`): `debtState(money, tier)` → `clear / watched / warned (45%) / final (75%) / sacked (100%)`. הגבולות: `[150k, 300k, 800k, 2M, 5M]` — בערך תקציב עונה, וליגה ג' סלחנית יותר כי שם לומדים. הבדיקה נעשית ב-`checkTheBooks()` **ברגע שהמאזן נסגר**, לא בסוף עונה.
- `Sacking` נשמר ב-state, `phase: 'sacked'`, מסך `Sacked.tsx` (מכתב מהבעלים + החוב/הגבול/מיקום), ו-`saveCareer` **מוחק את השמירה** — קריירה שנגמרה לא נמשכת.
- **דרך מילוט חובה**: `sellBlockedReason` מתיר מכירה גם כשהחלון סגור אם אתה במינוס. בלי זה זו מלכודת, לא לחץ.
- אזהרות: `DebtStrip` בדף הבית מתחת לכרטיס המשחק, עם המספר ופס התקדמות עד הקו.

⚠️⚠️ **הסרת הרצפה חשפה כלכלה שבורה מבנית.** מדידה ראשונה: **11 מתוך 12 קריירות חדשות פוטרו בעונה הראשונה.** ליגה ג' הפסידה ~230k לעונה מול תקציב פתיחה של 75k. השורש: **שכר של ₪620,000 לעונה בליגה ג'** — סותר ישירות את "ליגה ג' זה בחינם בלי חוזים". "גג 5K" זו התקרה של הכוכב, לא הממוצע, וההחלקה נתנה ממוצע 2,600.

**מה כויל** (הכל נמדד, לא נוחש):
- `WAGE_BAND` 1-2: רצפה 500/1,000 → **200/400**, וההחלקה `t <= 2 ? 2.8` (היה 1.4). ליגה ג' עכשיו: ממוצע **₪1,581**, שיא **₪4,300**, חשבון שבועי **₪25,300**. כל שאר הליגות נשארו בדיוק בטווח שביקשת.
- `START_MONEY` 75k → **180k**
- `seasonPurse` base: `[90k, 240k, 700k...]` → `[150k, 200k, 930k, 1.8M, 4.5M]`

```bash
npm run check:debt      # 29 בדיקות: הסולם, הפיטורים, המכתב, האזהרה לפני, ודרך המילוט
npm run check:economy   # שכר מול מה שהובטח + קריירה חדשה + כל ליגה
```
**אחרי:** 0/12 פיטורים בעונה ראשונה (ממוצע +54k, גרוע -105k מול גבול 150k). אמצע טבלה: ג' +77k · ב' +386k · **א' +54k (גרוע -399k)** · לאומית +352k · על +1,610k. הצורה: ליגה ב' היא שבה חוסכים, **ליגה א' היא שבה קפיצת השכר פי 6 חונקת**.

📌 **פתוח וחשוב: `STADIUM_START = 50`** — האצטדיון ההתחלתי מכיל 50 איש, כלומר הכנסות שער של **₪420 למשחק / 3k לעונה שלמה**. ההכנסה היחידה היא פרסי משחק, ומערכת הרחבת האצטדיון כמעט חסרת ערך בליגות הנמוכות. שווה טיפול.

**מדדים במספרים שלמים (31.8).** המורל הופיע כ-`76.36000000000001`. המקור: `coachMoraleBias` מחזיר שבר (`(motivation-8)*0.3 + (determination-8)*0.12`), והמדד פשוט צבר אותו.

**התיקון הוא באחסון, לא בתצוגה** — עיגול בציור היה מסתיר את אותו באג בכל מקום אחר שבו הערך נשמר. `meter()` ו-`cash()` ב-state.ts (16 + 12 מקומות), ומיגרציה ב-`loadCareer` שמנקה שמירות קיימות.

```bash
npm run check:numbers   # 56 מחזורים, 4 עונות, בדיקה אחרי כל מחזור
```
⚠️ **הבדיקה חייבת להריץ מאמן עם הטיה שברית ולבדוק אחרי כל מחזור.** גרסה ראשונה שלה בדקה רק בסוף העונה, המורל היה צמוד ל-100, והתקרה הסתירה את השבר — היא עברה בזמן שהבאג חי. אומת שהיא **נכשלת** על הקוד הישן (70.78, 71.56, 90.78...).

⭐⭐⭐ **אין שער בלי החלטה שלך (31.8, בקשת איציק).** "לא הגיוני שיש שער אם לא היה קטע של פעולה של המשתמש."

**מה נמצא במיפוי:** ארבעה מקומות מזיזים את התוצאה ב-`liveMatch.ts`. שניים אחרי החלטה (`scoreForPlayer`/`concede`), ו**שלושה נתיבים שקטים**: הזדמנות של היריב מתחת לסף, הזדמנות ברורה של היריב שנפלה בהגרלת 28%, והזדמנות שלך עם `q ≤ 0.36` ("resolve without bothering the player"). מדידה: **47% מהשערים (1.21 מתוך 2.59 למשחק) קרו בלי שנשאלת.**

**הכלל שנקבע:** הזדמנות שיכולה להסתיים ברשת **תמיד** עוברת אליך. הזדמנות מתחת לסף **לא יכולה להבקיע בכלל** — היא הופכת לשורת פרשנות (`NEAR_MISS`), נותנת חיים לטיקר ולמגרש, ולא נוגעת בתוצאה.

**מה השתנה:**
- `ATT_MOMENT = 0.13` / `DEF_MOMENT = 0.13` — הספים שעליהם מכוילת התוצאה.
- `oppChance`: הגרלת ה-72% **הוסרה** — הזדמנות מסוכנת של היריב היא תמיד רגע הגנה שלך.
- ⭐ **`Moment.q` חדש**: איכות ההזדמנות זורמת לתוצאה דרך `edge(q)`. קודם `resolveShot` השתמש ב-0.62/0.18 **קבוע** — הזדמנות זבל והזדמנות זהב הבקיעו באותו סיכוי. בגלל זה אי אפשר היה להוריד ספים בלי לנפח את התוצאה.
- כיול מלא של הסיכויים בשני הצדדים (`shotHit`, `gkRush`, `slideWin`, `beaten`, `twoV1`...).

```bash
npm run check:goals    # 400 משחקים, כל רגע נענה
```
**לפני:** 2.59 שערים, 47% שקטים, **3.88** החלטות למשחק.
**אחרי:** 2.59 שערים, **0% שקטים**, **7.54** החלטות למשחק. סגלים שווים בבית: **1.50-1.09, 46/27/27** (בכדורגל אמיתי נצחון ביתי ~45%). 66 מול 52: 85% נצחונות. אומת גם באפליקציה עצמה — שער נכנס, ובא אחרי החלטה.

⚠️ **`simulateMatch` (מנוע הליגה) לא נגעתי בו** — שאר הליגה מסומלצת כמו קודם, רק המשחק שלך השתנה.
📌 **פתוח:** ליריב אין נתיב פנדל, כלומר אין רגע הגנה של "לעצור פנדל". שווה להוסיף.

⭐⭐ **סבב שלישי (30.8): המגרש מוביל, לוח התוצאות עוקב.** איציק: "היה גול אבל הכדור היה באמצע המגרש". זה **לא היה באג בכדורגל — זו הייתה הארכיטקטורה**, וזה נוצר מהתיקון שלי בסבב הקודם.

`L.step(st)` מעדכן **תוצאה + באנר + כתובית באותו פריים**. אחרי שהחלפתי טלפורט בבניית התקפה (0.5-1.5 שניות), נוצר פער: התוצאה מתהפכת ו"גול!" קופץ **בזמן שהכדור עוד באמצע המגרש**. אי אפשר לתקן את זה בטלאי — המגרש רדף אחרי הפרשנות.

**עכשיו הכיוון הפוך:**
- `LivePitch` מקבל `play={id,home,scored}` ומחזיר `onPlayed(id,scored)` **בפריים שהכדור מגיע** (`PitchSim.onPlayed`, נורה מ-`land()` דרך `B.settle`).
- `Match.tsx` מחזיק `shown` (כמה מרשימת האירועים מותר לראות) ו-`shownScore`. **על שער**: השעון נעצר, התוצאה והכתובית מוחזקות, המגרש בונה את ההתקפה, ורק ב-`onPlayed` הכל משתחרר יחד. **על הזדמנות**: לא עוצרים כלום (שום מספר לא זז), רק מנגנים ברקע.
- `PitchSim.event()` מחזיר `boolean` — מסרב כשמהלך כבר באוויר, והקורא שואל שוב בפריים הבא.
- **רשת ביטחון קריטית**: `setTimeout(reveal, 3200)`. טאב ברקע לא מריץ rAF, והמשחק **אסור** שייתקע על פריים שלא יגיע. גם ל-`prefers-reduced-motion` יש שחרור דרך `setInterval`.

```bash
npm run check:sync    # 400 מהלכים: 0 שלא חזרו, 0 בצד הלא נכון, 0 "שער" בלי כדור בשער
```
הכתובית מוחזקת חציון **0.92 שניות** (גרוע ביותר 2.93 — מתחת לטיימאאוט). **ווב לייר**: זנב של 6 נקודות אחרי הכדור + הבזק רשת בצד שקלט.

⚠️ **אל תחזיר את `LivePitch` לקרוא `st.events` בעצמו.** זה בדיוק מה שיצר את חוסר הסנכרון. הוא מקבל הוראה אחת ומחזיר תשובה, נקודה.

**סבב שני (30.8, סרטון שני מאיציק: "הכדור עובר את השוער וזה לא שער", "השער מגיע משום מקום", "מסירות בין השוער לחלוץ"). שלושה באגים, כולם אמיתיים:**

1. ⭐ **כל אירוע ברשימה שיחזר את השער האחרון.** `LivePitch` השווה **אורכי רשימה** (`s.events.length !== seen`) ואז חיפש אחורה **בכל ההיסטוריה** את השער האחרון. כל שורת אווירה, כרטיס, חילוף — הפעילו מחדש את השער מדקה 9. זו הסיבה ל"שער משום מקום" ול"אין קשר לכתוביות". עכשיו `seen` הוא **סמן קריאה**, ורק אירועים אחריו נספרים. הלוגיקה יצאה ל-`eventToPlay()` ב-pitchSim כדי שתהיה בת-בדיקה: 7 מקרי בדיקה ב-`npm run check:pitch`, ואומת שהם **נכשלים** על הקוד הישן.
2. ⭐ **כל בעיטה עפה לקו השער.** גם כשלא היה שער — הכדור חצה את הקו בין הקורות ואז הפך לבעיטת שער. חזותית: הכדור נכנס ולא נותנים שער. עכשיו `aimShot()` מכוון בכנות: **שער** → בין הקורות (0.45-0.55, שם הן מצוירות); **הצלה** → נעצר על השוער, `goal - 0.05`; **החטאה** → חוצה את הקו אבל בבירור מחוץ לקורה; **חסימה** → מת לרגלי הבלם. מדד חדש `ball in the net, no goal`: **222 בקוד הישן, 0 עכשיו**.
3. ⭐ **`event()` טלפרט את הכדור.** הוא מסר ישירות לחלוץ הקרוב לשער — מרגלי השוער זו **מסירה אחת של 79% מהמגרש**. זה מה שנראה כמו "השוער מוסר לחלוץ", והשער באמת קפץ משום מקום. עכשיו `event()` **חמוש בלבד**, ו-`act()` מוליך את הכדור למעלה במסירות רגילות (`B.build`, עד 4) ובועט רק מ-0.26 מהשער. הרצף שנמדד: כדור ב-22% ← כתובית ← 46% ← 79% ← בעיטה ל-97% ברשת ← פתיחה מהמרכז ב-50%. השהיה כתובית→בעיטה: חציון 0.53 שניות.
4. גם: הכדור הארוך של השוער כוון ל**מתקדם ביותר** = החלוץ. הוגבל ל-0.52 מהמגרש. מדד: 79.3% → 53.9%.

**3 באגים אמיתיים שאיציק דיווח עליהם (30.8) ומה שתיקן אותם:**
1. **כל 20 השחקנים ליד רחבה אחת.** הבלוק עקב אחרי הכדור 1:1 (`back=0.06+att*0.46`, `front=0.45+att*0.50`) — שני הבלוקים התכנסו לאותם 40% מהמגרש. עכשיו `playX` **נמשך למרכז** (`0.5+(B.x-0.5)*0.78`) ו**מפגר** אחרי הכדור, והבלוק **א-סימטרי**: `back=0.07+att*0.42` (עד קו האמצע בלבד), `front=0.42+att*0.53`. מגן = בלוק קצר ועמוק עם איש למעלה; מתקיף = בלוק מתוח.
2. **המשחק חי בתוך רחבה אחת.** בעיטה → קרן → הרמה → בעיטה → קרן, לולאה. נוסף מצב **`clear`** (הגנה מרחיקה לקו האמצע לאגף) שהוא ~45% מהבעיטות, ותקרה של 2 קרנות רצופות.
3. **שוער מוסר לחלוץ והוא מחזיר לשוער.** השוער היה בועט `rx=null` והכדור נחת לידו → הוא עצמו הכי קרוב → אסף אותו בחזרה. עכשיו הוא **תמיד מוסר לאיש** (קצר לקרוב / ארוך לרחוק), ובכדור חופשי לשוער יש **פנדל מרחק של 0.24** כך שהוא לא אוסף כדור שהוא בעצמו שחרר.

**המספרים אחרי התיקון** (36 ריצות של 90 דקות): כדור בשליש הגנתי 21% / אמצע 55% / התקפי 24% · הזמן הארוך ביותר שהכדור לא יצא מרחבה **6.9 שניות** · פרישת 22 השחקנים **84% מאורך המגרש** · שוער→חבר→שוער **0** · שוער שכיוון לשוער השני **0** · שערים שנרשמו באמצע המגרש **0** · 42% מהמסירות מגיעות לאגפים.

⚠️ **אל תשנה את `back`/`front`/`aim` ב-`pitchSim.step` או את `shotOut()` בלי להריץ `npm run check:pitch`.** אלה בדיוק המספרים שהמשחק נשבר עליהם.

**3 מערכים** (`src/data/formations.ts`, מקור אמת יחיד — המסך, המנוע והמגרש קוראים ממנו): **4-4-2 מאוזן** (att 1.00 def 1.00), **4-3-3 התקפי** (1.06 / 0.95), **5-4-1 הגנתי** (0.90 / 1.09). כל אחד מחזיק 11 סלוטים `{d,y,line,brk}` — `d` הוא העומק בתוך הבלוק ולא מקום מוחלט על המגרש, ו-`brk` הוא איך הסלוט פורץ שורה כשהקבוצה למעלה. `fillFormation` משבץ 11 אנשים ב-11 סלוטים, וקו חסר לווה מהקו שלידו (חמישייה מאחור לוקחת קשר עודף כמגן כנף). AI מקבל מערך קבוע לפי hash של מזהה המועדון. **מאוזנים**: 4-3-3 מול 5-4-1 = 37.8/26.5/35.7 — יתרון של 2 נקודות, אבל 4-3-3 מייצר משחק פתוח (1.56 שערים) ו-5-4-1 חונק (1.08). הצורה נשמרת במגרש: פער הגנה→קישור 15.7% מהמגרש, קישור→התקפה 16.3%, הקווים בסדר הנכון ב-99.9% מהפריימים.

⭐ **דף הבית שוכתב (30.8, בקשת איציק: "נראה עמוס מדי במלל... אין לי דחף להישאר").** מה שהיה: 9 אריחים ברוחב מלא, כל אחד אייקון + כותרת + משפט אפור שמסביר את עצמו + חץ. הכל באותו משקל, אז כלום לא נקרא כחשוב, המשפטים המסבירים היו ה"מלל", ו**הכפתור שמתחיל את המחזור ישב בתחתית, אחרי 1,900 פיקסל של גלילה**.

עכשיו הדף עונה על שלוש שאלות בסדר הזה ורק עליהן:
1. **על מה אני משחק** — `seasonGoal()` ב-state.ts (חדש), ורצועת `GoalStrip`: סולם של המקומות בליגה, שתי המשבצות העליונות זהב, האחרונה אדומה (**רק אם יש בכלל לאן לרדת** — בליגה ג' אין), המשבצת שלך דולקת, ומשפט אחד מתחת. המשפט תמיד נותן משהו בר-השגה: אם העלייה כבר לא ריאלית (`need > left*2`) הוא עובר ל"עקפו את גבעתיים (6 נקודות מעליכם)". הכותרת הופכת ל"להישאר בליגה" כשאתה באזור הירידה.
2. **מי הבא** — `MatchHero`: סמלים 52px, מיקום בטבלה לכל צד, צ'יפ בית/חוץ/דרבי, ו**הכפתור בתוך הכרטיס**. עלה מ-1,900px ל-**477px, מעל הקיפול**.
3. **מה עוד יש** — רשת אחת של 8, מילה אחת לכל תא, ו**נקודה צבעונית במקום משפט** כשמשהו דורש תשומת לב. תיבת ההודעות היא ההתראה היחידה שעדיין מדברת במילים, כי היא היחידה שחוסמת.

ההיסטוריה (אוהדים, פיד, טבלה) מתחת. הטבלה עצמה לחיצה ופותחת את הליגה המלאה, אז ירד לה אריח. `StickyStart` מחזיר את הכפתור בתחתית אחרי שהכרטיס נגלל החוצה.

⚠️ **מה שאי אפשר לאמת בפאנל של קלוד:** `requestAnimationFrame`, `IntersectionObserver` **ואירועי scroll** — כולם קשורים ללולאת הרינדור, והדף לא מרנדר שם. לכן `StickyStart` לא נבדק. הוא כתוב כך שכשל שלו בטוח (`show` נשאר false והסרגל פשוט לא מופיע, כי אותו כפתור כבר בכרטיס למעלה). **אל תחליף אותו למאזין scroll, גם זה לא נורה שם.**

**מה כן נמדד:** גובה 1957→1738px, טקסט 1085→1008 תווים, ה-CTA 1900→477px, אין גלילה אופקית ב-375px, ניגודיות 7.1:1 ו-7.78:1 על תוויות הרשת ושורת המטרה, וכל אזורי המגע ≥44px (צ'יפי הפילטר בפיד היו 32px, הורחב שטח הנגיעה ב-`::after` בלי לשנות את המראה). באג bidi שנתפס: "מקום 1/8" התהפך ל-8/1, תוקן עם `.num` (isolate LTR).

**דילמות הורחבו** (`src/data/dilemmas.ts`) מ-9 ל-29 תבניות, 10 דוברים. **מודעות לסגל**: יודעות מי באמת יושב, מי הכישרון הצעיר, המבקיע, החלוץ בבצורת, המקום בטבלה, אם דרבי. חלוקה ל**דחוף** (חוסם לפני המשחק) מול **תיבת הודעות** (`inbox`) שמחכה ב-Hub עם באדג' אדום (`src/ui/screens/Inbox.tsx`).

**הודעות וואטסאפ אחרי משחקי קיצון** (`src/data/chats.ts` + `src/ui/screens/Chat.tsx`). מסך שמחקה וואטסאפ אמיתי (טפט דודל, "מקליד...", בועות עם זנב, וי כפול כחול, חותמות זמן). מופעל רק בדרבי / רצף 3 / הפרש 3+ שערים (~31% מהמחזורים). ניצחון = אוהדים+קפטן חוגגים, הפסד = מנכ״ל/בעלים דורשים הסבר.

**מסך ליגה מלא** (`src/ui/screens/Standings.tsx`, כניסה מ-Hub): טבלה מלאה + **מלך השערים** + **מלך הבישולים**, לכל הליגה (לא רק הסגל שלי). **הסטטיסטיקה נאספת לכל הליגה** ב-`commitRound` (`seasonStats` עם `name/clubId/apps/goals/assists`). **בישולים** נשמרים (המנוע כבר בחר מבשל, רק לא שמר, תוקן ב-`matchEngine.ts` + `liveMatch.ts`). **היסטוריית קריירה** (`careerStats`) נשמרת בסוף עונה, ומוצגת בכרטיס השחקן (`PlayerCard.tsx`) כטבלת עונות.

**כלכלה, הוצאות שוטפות** (`career.ts` → `roundCosts`): שכר שבועי + תחזוקת מגרש + אבטחה (בית), נגבה **כל מחזור** ב-`commitRound`. הוסר החיוב הכפול בסוף עונה. **"מאזן שבועי"** מוצג על מסך התוצאה (`Result.tsx`, `RoundLedger`). ניצחון בית בג' ≈ +11K, תיקו -39K, הפסד -61K. עונה גרועה מפסידה כסף.

**טקטיקה שמשנה את המשחק** (`liveMatch.ts` → `TacticMode`, `MODE_MOD`): "אול אין" (att×1.15, def×0.83, הימור פרוע) ו"לחץ גבוה" (att×1.13, def×0.96, הכי מנצח אבל שורף כושר). שכבה חיה בלבד, לא נוגע במנוע המכויל. אומת ב-600 משחקים.

**חגיגת אלופים בסוף עונה** (`SeasonEnd.tsx` → `Celebration`): קונפטי קנבס + זום + הבזק + כותרת "אלופים". רק לאלוף/עולה (אמצע/ירידה → מסך רגיל). `public/celebration/champion-tier1.webp` קיים. **חסר: tier2 (על) ו-tier3 (אירופה), הפרומפטים כבר נכתבו ומחכים לאיציק לייצר.**

**תיקון RTL של כל החצים:** קדימה/פתיחה = שמאלה (`‹` / chevron ברירת מחדל), חזרה = ימינה (`›` / scaleX(-1)). מספרי "מ→ל" עכשיו ב-`dir="ltr"` עם חץ אלכסוני `↗`/`↘`.

**נכסים/דברים לזכור:**
- **תצוגות dev זמניות בקוד** (למחוק לפני השקה): ראוטים `?pens ?fks ?shots ?one ?defk ?deft` (גלריות רגעים), `?chat=<trigger>`, `?league`, `?champ=<tier>`, `?stadium=<1-4>` (4 דרגות האצטדיון). פונקציות dev ב-`state.ts`: `demoChat`, `demoSeason`. גלריות ב-`App.tsx` (`MomentGallery`).
- Higgsfield: `nano_banana_pro` / kling. חשוב "blank kit, no logos". בלי פנים בדמויות.

---

## 🎯 שלושת היעדים הבאים (לפי סדר, איציק ביקש מפורשות)

**1. ✅ בוצע ואומת. חלון העברות פרה-סיזן של 3 מחזורים.** ראה "עדכון הסשן האחרון" למעלה. הכל בנוי: סאגות עזיבה (כוכב + צעיר), חידוש חוזים, שוק פתוח, נעילת חלון קיץ בעונה, top-up נוער. **הרחבות אפשריות בעתיד:** גיוון בסאגת הכוכב (עכשיו אותו "הכי טוב רוצה לעזוב" כל קיץ), סאגת "קבוצה גדולה מריחה אותך" בדרג העליון, עוד ארכיטיפים של בקשות (ותיק שרוצה עוד עונה, שחקן שרוצה לעזוב כי לא משחק).

**2. ✅ בוצע ואומת. מאגר אוהדים גדול עם אישיויות.** ראה "עדכון הסשן האחרון" למעלה. `fans.ts` שוכתב, 11 אוהדים עם voice (כולל היודע-כל המבוגר), 45+ שורות עם סלוטים, אנטי-חזרתיות דרך `fanHistory`. **נותר אם רוצים:** `chats.ts` (שיחות הוואטסאפ אחרי משחקי קיצון) עדיין 10 שיחות, אפשר להרחיב באותה שיטה. גם: לתת ל-voice של האוהד עקביות פר-אוהד לאורך עונה (עכשיו נבחר אקראית פר הודעה).

**3. ✅ בוצע ואומת. בניית אצטדיון עם 4 תמונות + הכנסות שער + חסימת עלייה מדרג 3.** ראה "עדכון הסשן האחרון" למעלה. **הרחבות אפשריות:** תמונות בצבעי מועדון (עכשיו נייטרלי), אנימציית "האצטדיון גדל" במסך התוצאה, חסויות/שדרוגים נוספים (VIP, חניון), כיול מספרים (מחירי כרטיס/עלויות בנייה) בסימולציית קריירה מלאה.

---

## איפה הכל יושב

- **קוד הפרויקט:** `C:\Users\itzik\Desktop\CLAUDE\be-a-pro`
- **סקיל המומחיות:** `~/.claude/skills/be-a-pro-skill` (יטען אוטומטית לפי המילים מנג'ר, סגל, קלפים, מנוע משחק וכו')
- **מסמך התכנון שנשלח לאייל עוזיאל:** `Desktop/CLAUDE/be-a-pro-plan.pdf` ו-`.html`
- **תמונות הרגעים ב-Higgsfield:** `be-a-pro/public/moments/*.webp`

## להריץ

```bash
cd C:/Users/itzik/Desktop/CLAUDE/be-a-pro
npm run dev
```

השרת עולה על `http://localhost:5180/`. אם התפוס, Vite יקפוץ לפורט הבא ויכתוב אותו בטרמינל.

**סקריפטים:** `npm run dev` פיתוח, `npm run build` בילד, `node_modules/.bin/tsc --noEmit -p tsconfig.json` בדיקת טיפוסים.

## הסטאק (נעול)

React 18 + TypeScript + Vite. מנוע הסימולציה TypeScript טהור בלי DOM. אין Zustand, כל הסטייט ב-App כ-`useState`. שמירה ב-`localStorage` דרך `src/game/save.ts`. בהמשך: Capacitor לאריזה ל-iOS ואנדרואיד. **לא Unity**, זו החלטה סגורה.

---

## מבנה תיקיות (מה יושב איפה)

```
be-a-pro/
├── public/moments/          תמונות הרגעים (penalty, shot, one-on-one, tactic)
├── public/stadium/          4 תמונות אצטדיון tier1..4.webp (מקור PNG ב-PIC/איצטדיונים)
├── src/
│   ├── engine/matchEngine.ts        מנוע דטרמיניסטי, מכויל 2.7 שערים/משחק
│   ├── game/
│   │   ├── state.ts                 GameState + כל הטרנזיציות
│   │   ├── liveMatch.ts             מנוע חי דקה-דקה (עם רגעים, חילופים, מחצית)
│   │   ├── league.ts                לוח משחקים + טבלה
│   │   ├── transfers.ts             חלונות העברות + חוזים (חורף בלבד בעונה)
│   │   ├── preseason.ts             חוזים (contractYears/seedContract/renewTerms) + מחולל סאגות הקיץ
│   │   ├── chronicle.ts             זיהוי רגעים היסטוריים בקריירה
│   │   ├── traitEffects.ts          השפעה קטנה של תכונות על המשחק החי בלבד
│   │   └── save.ts                  שמירה ב-localStorage
│   ├── data/
│   │   ├── clubs.ts                 8 מועדונים מומצאים עם אופי + דרבים
│   │   ├── managers.ts              (שרידי, לא בשימוש UI ישיר)
│   │   ├── names.ts                 מאגר שמות יהודי + ערבי (85/15, לא מתערבבים)
│   │   ├── squadGen.ts              יצירת סגלים
│   │   ├── dilemmas.ts              תבניות דילמה עם slots
│   │   ├── press.ts                 שאלות מסיבת עיתונאים לפי מצב
│   │   ├── personalities.ts         58 תכונות אופי, נגזרות מה-id, סלוטים + חבילת שישי
│   │   └── fans.ts                  11 אוהדים עם voice (sage/doom/nostalgic/...), 45+ שורות + סלוטים + אנטי-חזרתיות
│   └── ui/
│       ├── tokens.css               מערכת עיצוב "אור זרקורים"
│       ├── App.tsx                  ראוטר של הפיזות
│       ├── components/
│       │   ├── Crest.tsx            סמלי קבוצות SVG (4 צורות, 5 דפוסים)
│       │   ├── Icon.tsx             משפחת אייקונים ווקטורית, 24 אייקונים
│       │   ├── bits.tsx             Meters, ScorePair (RTL fix), StatBox
│       │   ├── FanNote.tsx          בועת הודעת אוהד
│       │   ├── PlayerCard.tsx       כרטיס שחקן מלא (אופי, כושר, יכולות)
│       │   ├── Stepper.tsx          סרגל 5 שלבים באונבורדינג
│       │   └── Tutorial.tsx         מסך הסבר לפני מחזור 1
│       ├── components/AssistantNote.tsx  בועת העוזר מאמן (חוזר על האפשרויות)
│       └── screens/
│           ├── Captain.tsx          בחירת קפטן, מדורג לפי דירוג+ותק
│           ├── Assistant.tsx        גיוס/ניהול עוזר מאמן
│           ├── Title.tsx            מסך פתיחה עם אצטדיון CSS
│           ├── Onboard.tsx          שם+כינוי, אז בחירת מועדון
│           ├── Signing.tsx          מסיבת עיתונאים של חתימה
│           ├── Squad.tsx            סגל 16 שחקנים + PlayerRow משותף
│           ├── PreSeason.tsx        לוח הקיץ, 3 מחזורי פרה-סיזן (PreSeasonMarket)
│           ├── Stadium.tsx          מסך האצטדיון, 4 תמונות + בנייה + הכנסות שער + דרישת עלייה
│           ├── Transfers.tsx        שוק עם מסך חוזה
│           ├── Hub.tsx              בית: משחק הבא, פעולות, אוהד, טבלה
│           ├── Dilemma.tsx          דילמה בסגנון וואטסאפ
│           ├── Tactic.tsx           בחירת סגנון ולחץ
│           ├── Match.tsx            שידור חי + פופ אפי רגעים
│           ├── Table.tsx            טבלת ליגה מיני
│           ├── Result.tsx           תוצאה + אוהד + שאר המחזור
│           ├── Press.tsx            עיתונאי עם שאלה
│           ├── Chronicle.tsx        ציר זמן של רגעי הקריירה
│           └── SeasonEnd.tsx        סיכום עונה
```

---

## מה כבר עובד סוף לסוף

**זרימת אונבורדינג של 5 שלבים** (עם Stepper גלוי):
1. שם + כינוי (Onboard)
2. בחירת מועדון עם יתרון וחיסרון (Onboard)
3. מסיבת עיתונאים של חתימה עם הודעת בעלים (Signing)
4. הכרות עם הסגל (Squad)
5. פרה-סיזן, 3 מחזורי קיץ (PreSeason, `PreSeasonMarket`)
→ אז Tutorial קופץ ואז המחזור הראשון.

**מחזור** = חדר מנג'ר (דילמה) → טקטיקה → שידור חי → תוצאה → מסיבת עיתונאים → בית.

**קפטן** (`state.ts`: `captainId`, `captainScore`, `captainCandidates`, `currentCaptainId`, `setCaptain`; מסך `Captain.tsx`, כניסה מ-Hub): הסגל מדורג לפי `OVR + ותק` (גיל מעל 21, מוגבל ל-14 שנים ×0.9), אז ותיק מנצח צעיר עם דירוג מעט גבוה יותר. `currentCaptainId` נופל למועמד הראשון אם לא נבחר ידנית. אות "C" זהב (`CaptainMark` ב-Squad.tsx) מוצג על הקפטן בשורות הסגל. השפעה קטנה: קפטן בהרכב = מורל +2 לקבוצה במשחק החי (ב-`liveMatch.createLive`, מקבל `captainId` מ-`liveMatchInput`).

**עוזר מאמן** (`state.ts`: `Assistant`, `hireAssistant`/`fireAssistant`/`assistantActive`/`assistantEcho`/`maybeAssistantDeparture`; מסכים `Assistant.tsx` + `components/AssistantNote.tsx`): דמות קומית, אוהב כדורגל ולא מבין כלום. מגויס מ-Hub (חינם). **מנגנון:** `assistantEcho(gs, options)` מחזיר את האפשרויות של השחקן בחזרה כ"עצה" חסרת ערך ("אני הייתי הולך על [א]. או [ב], תלוי איך אתה מרגיש"). `AssistantNote` מוצג ב-Dilemma וב-Tactic מתחת לאפשרויות. **עוזב בליגה א'**: `maybeAssistantDeparture` (נקרא ב-`enterSeason`) מסמן `departed` ומוסיף פרק כרוניקה כשהמועדון מגיע ל-`tier >= ASSISTANT_LEAVES_TIER` (3). **הערה: עדיין לא נגיש בפועל**, כי עלייה בסוף עונה מריצה `newGame` (חוזר ל-tier 1). יתפוס כשתיבנה התקדמות רב-עונתית שנושאת את ה-tier.

**קריירה רב-עונתית, שלב 2 מתוך 4 (בוצע חלקית)**

המסלול שאיציק הגדיר: ישראל ג׳→ב׳→א׳→לאומית→על. **בליגת העל רק מקום ראשון** פותח עונה נוספת בעל plus מסע אירופי. זוכים בגביע אירופה → עוברים לאמן בחו"ל: דרג כניסה (הכוכב האדום/קלאב ברוז׳/אולימפיאקוס) → אליפות מקומית + גביע אירופה → דרג ביניים (לברקוזן/רומא/וייאריאל/ליון/אסטון וילה) → אליפות + אירופה → צמרת עולמית (ריאל, ברצלונה, יונייטד, ליברפול, ארסנל, צ׳לסי, סיטי, באיירן, אינטר, מילאן, פ.ס.ז׳). **החלטות שאיציק קיבל:** ירידת ליגה + אזהרת בעלים, מצב מורשת בלי סוף בצמרת.

**⚠️ חסם לפני השקה לחנויות:** איציק בחר שמות מועדונים אמיתיים **לפרוטוטיפ בלבד**. `references/legal-and-data.md` בסקיל אומר מפורשות ששם וסמל מועדון הם סימן מסחר ואסורים בלי הסכם. **חייבים להחליף לשמות בהשראה (עיר וצבעים) לפני פרסום.** הוצעו כבר: אדומי בלגרד, אדומי פיראוס, צהובי רומא, סגולי ברמינגהם, לבני מדריד, תותחני צפון לונדון.

**מה נבנה (מערכה 1, ישראל):**
- `data/leagues.ts`: 32 מועדונים בדיוניים ל-4 הליגות מעל ג׳ (8 לכל דרג), בונים דרך `build(tier, spec)` שמדרג יוקרה ותקציב לפי דרג. `clubsForTier(tier)`, ו-`poolForTier` ב-career.ts מחזיר את LEAGUE_C לדרג 1.
- `game/career.ts`: **הזדקנות** (`ageSquad`), עקומת גדילה לפי גיל, **תקרת פוטנציאל נסתרת** (`potentialOf`, 45-90 מ-hash של ה-id), פרישה (37+ תמיד, 35+ בהסתברות), נוער שעולה למלא חוסרים, שוערים על עקומה מוזזת 3 שנים, `DECLINE_BIAS` (מהירות נופלת ראשונה, מסירה אחרונה). **כלכלה**: `playerWage` (ovr^2.2 × 5.5), `wageBill`, `seasonPurse` עם בונוס עלייה ×2, `matchPrize` לפי דרג. **`buildNextSeason`** מגלגל עונה: עלייה (1-2), ירידה (אחרון), הסגל עובר ומזדקן.
- **כל הליגה מתמידה ומזדקנת**, לא רק השחקן. `aiOffSeason` מזקין סגל AI ואז מחתים 1-3 שחקנים להחזיר אותו לרמת הדרג.
- `state.ts`: `season`, `lastReport`, `startNextSeason`, `atTopTier`, פרק כרוניקה לעלייה/ירידה. חוב נחסם ב-0 עם קנס מורל 10- ומעמד 4-.
- מסכים: `SeasonEnd` שוכתב (עלייה/ירידה, טבלה מסומנת), ופאזה חדשה **`preseason`** = מסך "הקיץ" עם המאזן ודוח מי פרץ/דעך/פרש/עלה מהנוער.

**גוצ׳ות שנתפסו בסימולציה, לא בעין:**
- קבוצות AI היו מקבלות סגל חדש בשיא הכוח כל עונה בזמן שהשחקן רק מזדקן. ליכון בלתי אפשרי, אף פעם לא עולים. תוקן בהתמדה + `aiOffSeason`.
- בלי תקרת פוטנציאל, צעירים גדלים לנצח והליגה מנפחת את עצמה מעל תקרת הדרג (54→59 בדרג שתקרתו 53).
- `leagueCeiling` הוא קלט נומינלי, ה-OVR בפועל יוצא כ-5 נקודות מעליו (כי `attrFor` נותן `hi()` לתכונות המפתח). לכן הצעד הוקטן מ-7 ל-5.5 לדרג.
- `SeasonEnd` הציג `lastReport` שהוא מהעונה **הקודמת**, כי ההזדקנות קורית רק ב-`startNextSeason`. לכן הדוח הועבר למסך `preseason`.
- **אימות**: סימולציות קריירה מלאות (מנהל אקטיבי שקונה בשוק). זרע 4242 הגיע לליגת העל ב-18 עונות, זרע 777 באלוף ליגת העל בעונה 13, זרע 31337 נתקע בלאומית גם אחרי 40 עונות. שונות בריאה, אי אפשר לסיים ביומיים.

**נותר במסלול:** מסע אירופי עם הקבוצה הישראלית, ואז שלושת דרגי חו"ל. גם: שוק ההעברות לא גדל מספיק בדרגים גבוהים, ב-31337 נצברו 96M בלי מה לקנות, לטפל בשלב הכלכלה.

**קושי ומחשבה, ציר 1 מתוך 4** (איציק ניצח 4-0 במשחק ראשון, "שלא יהיה קל, שיגרום לחשוב". רודמאפ מוסכם: 1 עומק בכל משחק [בוצע], 2 קריירה רב-עונתית, 3 יעד בעלים+פיטורים, 4 פיתוח שחקנים+כלכלה):
- **המודיעין הפך לניחוש (התיקון העיקרי).** קודם השוער צלל *בדיוק* לכיוון הרמז, אז "בחר פינה הפוכה" = שער חינם, זה מה שנתן 4-0. עכשיו `actualDive(st, hint)` ב-liveMatch: השוער צולל לכיוון הרמז ~60%, אחרת אקראי אחר. בחירת פינה = קריאה עם סיכון.
- **סיכויי רגעים הונמכו:** הזדמנות 0.8→0.62, אחד-על-אחד 0.5/0.46→0.44/0.40, פנדל 0.92/0.3→0.85/0.25. בעיטה חופשית נשארה 0.3/0.08. כולם עוברים דרך `actualDive`.
- **דוח סקאוטינג + טקטיקה משמעותית** (`matchupScout` ב-state.ts, `ScoutTile` ב-Tactic.tsx): לפני המשחק מוצג דירוג הסגל שלך מול היריבה, פייבוריט/שקול/אאוטסיידר (עם הטיית בית), ושורת עצה שמזהירה מהתקפי-מדי מול קבוצה חזקה. הטקטיקה כבר משפיעה במנוע (APPROACH_MOD att/def ±6% ב-teamRatings), הסקאוטינג גורם לשחקן להשתמש בזה.
- אימות headless (600 משחקים, "שחקן חכם" שבוחר תמיד הפוך מהרמז, סגלים שווים, בית): שערים 1.68, ספיגות 1.17, W/D/L 50/25/26, בלואאוטים (ניצחון 3+) רק 12%. 4-0 הפך לנדיר במקום ברירת מחדל. **המנוע המכויל (שאר המחזור) לא נגע.**

**אורך וצפיפות המשחק** (עודכן לבקשת איציק, "להגדיל את המשחק"): `MS_PER_MIN` ב-`Match.tsx` הועלה 620→**780** (משחק שלם ~93 דק' = **~73 שניות ב-x1**, 36 ב-x2, 24 ב-x3). נוספה **פרשנות אווירה** (`ambientCommentary` ב-liveMatch, type `'ambient'`) שממלאת את הפיד בכ-27 שורות למשחק (קרנות, עצירות שוער, כדורים ארוכים, יציע ששר), ~38 אירועי פיד למשחק. נוסף **רגע החלטה חדש, בעיטה חופשית** (kind `'free_kick'`, `resolveFreeKick`, מרנדר עם `shot.webp` וקיקר "בעיטה חופשית", אקסנט sky). ההחלטות עלו מ~2 ל~3 למשחק. שערים/משחק עלו קלות ל~2.9 (בכוונה, מהרגעים ההתקפיים הנוספים), הבעיטה החופשית מכוילת קשה (מנצח שוער 0.3, אחרת 0.08). `ambient` מסונן ב-`finalize`.

**עמוד המשחק שוכתב (2026-08-20):** קודם `LineupPanel` היה פתוח כברירת מחדל ותפס את כל המסך, אז השידור החי נדחף מתחת לקו הקיפול (איציק דיווח שצריך לגלול כדי לראות מה קורה). עכשיו: לוח תוצאות למעלה, **השידור החי (feed) הוא האזור הראשי `flex:1`** ממש מתחתיו, ואז **רצועת כושר קומפקטית (73px)** של 11 ברים. לחיצה על הרצועה (או כפתור במחצית) פותחת **SubSheet**, bottom sheet ב-Portal עם ההרכב המלא + חילופים בשתי נגיעות. המשחק **נעצר כשה-SubSheet פתוח** (`running` כולל `!subOpen`). אומת ב-375px: feed 191-484 (מעל הקיפול), רצועה 73px, sheet עוגן לתחתית 130-812, אפס גלישה. הערה: אנימציית ה-sheetUp נראית "תקועה" ב-preview כי ה-pane לא עושה compositing, בנייד אמיתי היא רצה.

**מנוע משחק חי (`liveMatch.ts`)** רץ דקה-דקה עם:
- שליטה על מהירות (x1/x2/x3), עצירה
- שחיקת כושר מכוילת (בערך 45 בסוף המשחק)
- **מחצית בדקה 45** עם נאום שמשנה מורל לפי המצב
- **חילוף בשתי נגיעות**: לחיצה על שחקן פותחת הצעת מחליפים מדורגת לפי תפקיד, כל אחד עם ציון והפרש `+/-`
- **פאנל הרכב פתוח כברירת מחדל** עם ברי כושר + ספסל
- עד 3 חילופים

**סרטונים חיים לרגעים, פיילוט שנפסל (2026-08-20):** איציק רצה להחליף את התמונות הסטטיות בסרטונים (בילדאפ → בחירה → סרטון תוצאה). נעשה פיילוט מלא לפנדל: 4 תמונות מפתח + 3 סרטונים (9:16, Higgsfield kling3_0, "Stadium Gold Noir"), נשמרו ב-`pilot/penalty/` ו-`public/moments/penalty/`. חובר לרגע הפנדל ואומת שעובד (בילדאפ מתנגן, בחירה → תוצאה). **איציק אמר "לא טוב" ופסל.** הקוד הוחזר למצב תמונה סטטית (MomentPopup ללא video, אין OutcomeVideo, penalty rate הוחזר מ-5 ל-0.14). הנכסים נשארו ב-`pilot/` ו-`public/moments/penalty/` כרפרנס, לא בשימוש. גוצ'ות שהתגלו: (1) nano_banana_pro מוסיף סמלי ליגה/פרסום מזויפים לחולצות (סימן מסחר), צריך "blank kit, no logos". (2) העלות זולה, 6 קרדיטים לסרטון std 4s, כל 18 הקליפים ~110 קרדיט. **למחשבה בשיחה חדשה: למה זה נפסל (איכות? סגנון? התחושה?) ואיזה כיוון אחר.**

**רגעי הגנה (2026-08-20, בקשת איציק):** קודם כל הרגעים היו התקפיים והגנה הייתה פסיבית (`oppChance` פתר אוטומטית). עכשיו הזדמנות ברורה של היריב (q>0.4, ~0.72) הופכת ל**החלטה שלך**: `def_keeper` (החלוץ שלהם לבד מול השוער, לצאת אליו / להישאר על הקו) ו-`def_tackle` (חדירה מול הבלם, סליידינג / להישאר על הרגליים). הקריאה תלויה בתכונות: מול חלוץ כדרור עדיף להישאר, מול סיומן עדיף לצאת/לעכב. **סליידינג** שנכשל = עבירה (צהוב, ולפעמים אדום שמוריד את הבלם ל-10) או חלוץ שחדר. **להישאר על הרגליים** אינו בטוח: ב-~24% מהמקרים תוקף נוסף מצטרף ל**2 על 1** (`p2v1` תלוי בהתקפת היריב), ואז מסירה וגמירה. אז לכל אופציה סיכון משלה, סליידינג=עבירה, עמידה=עומס יתר, וזו קריאה לפי המצב (אתה שחקן אחרון? יש כיסוי? כמה החלוץ כדראי). `resolveDefKeeper`/`resolveDefTackle` ב-liveMatch, `concede()` מבקיע ליריב. אימות: 0.84 רגעי הגנה למשחק, שערים 2.64 (נשאר מכויל). ה-UI: `def_keeper`/`def_tackle` ב-MOMENT_LOOK (מאיירים one-on-one/tactic זמנית, אקסנט loss) ו-routing עם onPickOption.

**רגעי החלטה כפופ אפ מסך מלא** עם תמונות מ-Higgsfield:
- `penalty` (פנדל, שלוש פינות עם `direction: ltr` שיתאימו לצדדים)
- `shot` (הזדמנות)
- `one_on_one` (עגל את השוער / סיים בפינה)
- `tactic` (בפיגור: הכל קדימה / לשמור על הראש / ללחוץ גבוה)

**8 קבוצות בליגה ג׳** עם צבעים, סמלים ווקטוריים (Crest.tsx), 4 דרבים מוגדרים, ותכונות שמזיזות תקציב/הגנה/התקפה/נוער בפועל.

**סגלים מיוצרים** עם שמות ישראליים (85% יהודי, 15% ערבי, לא מתערבבים אף פעם), כישרונות צעירים מסומנים.

**שוק העברות** עם חלונות: קיץ מחזורים 1-3, חורף 8-9. חתימה דורשת אישור חוזה (דמי חתימה, שכר שבועי, אורך). מכירה מהספסל בלבד ב-80%.

**מסיבת עיתונאים אחרי כל משחק** עם שאלה מותאמת: תוצאה גדולה, תיקו, הפסד, ספיגה, דרבי, ראש טבלה, תחתית. 5 אתרים בדיוניים (ספורט WOW, 555, ישראל, Yספורט, Nספורט). טונים: קליל / ענייני / קוטל.

**ארבעה אוהדים ותיקים** (שמעון/רפי/מוקי/אלי צ׳יקו) עם הודעה לפני משחק ואחרי משחק, מגיבים למצב אמיתי: חלוץ שלא הבקיע, רצף הפסדים, דרבי, ראש טבלה.

**זהות מאמן צומחת** מהחלטות: `players/media/money` נספרים, ומתחת לשם מופיע תואר "איש של השחקנים" / "איש התקשורת" / "מנהל עם ראש על הכתפיים".

**מדדים** (בסקטור העליון): תקציב, מעמד, מורל. כולם משפיעים על המשחק בפועל.

**אופי שחקנים** (`data/personalities.ts`): 58 תכונות בעברית ישראלית אמיתית, כולל חבילת "יום שישי" (המשחקים בליגות ג'/ב'/א'/לאומית הם **שישי בצהריים**, אז האופי סובב סביב ליל חמישי לפני המשחק, חומוס של שישי בבוקר, ארוחת שישי, כניסה לשבת, מילואים, עבודה ביום, עולה חדש, כדורגל שכונה), מחולקות לקבוצות (לילה, מזג, תקשורת, אוכל, אמונות, ריצה, קומדיה, לב, גיל, שוער, כסף, קוריוז). **התכונות לא נשמרות על השחקן**, הן נגזרות דטרמיניסטית מ-hash של `id + name`, ולכן סייבים ישנים, שחקני שוק ושחקנים חדשים מקבלים אופי בלי מיגרציה ובלי לגעת ב-rng של המנוע. שתי הגנות מפני חזרתיות: (1) לכל שורה יש **סלוטים** שמתמלאים מ-picker לפי seed (רכב, שנה, יום, מנה, מספר), אז שני "חסכן" נוסעים ברכב אחר. (2) **`assignTraits(players)`** עובד על סגל שלם, אף תכונה לא חוזרת פעמיים, קבוצה עד פעמיים, ורק ~65% מהסגל מקבלים אופי, כשהחלשים (OVR<52) שקטים. `fit` מגביל התאמה (נער/ותיק/שוער/שחקן שדה), `signature: true` מסמן תכונה שמגדירה *סוג* שחקן ולכן מובילה. `renderLine(t,p)` מרנדר את השורה עם הסלוטים.

**פוטנציאל בכרטיס** (`career.ts`: `reachableCeiling`, `potentialBand`): מתחת לדירוג בכרטיס מוצג "עד lo-hi" בירוק קטן, הערכת סקאוט לתקרת ה-OVR. נגזר מהפוטנציאל הנסתר `potentialOf` וחסום גם לפי גיל (נער עד +13, בן 25 +4, בן 30 +1, בן 33+ "peak" בלי טווח). טווח רך (1-2 רוחב, דטרמיניסטי מ-id) כדי שירגיש הערכה ולא הבטחה. שחקן שכבר מעל הפוטנציאל שלו לא מציג טווח.

**כרטיס שחקן** (`components/PlayerCard.tsx`): נפתח מ**לחיצה כללית על השורה** (בקשת איציק). מוביל עם מי הוא (תכונות אופי + ציטוטים), אחר כך כושר/מורל, מאזן העונה מ-`seasonStats`, ורק בסוף היכולות המספריות. מקבל `traits` prop מ-`assignTraits` כדי שהכרטיס יתאים לשורה. שוער מקבל את מאפייני השוער.

**PlayerRow** שוכתב: גוף השורה = כפתור שפותח כרטיס (`onOpen`), והחלפת הרכב עברה ל**כפתור חצים בצד** (`swap`/`onSwap`, אייקון sub). `traits` prop סמכותי: מערך ריק = בלי אופי (שחקן לא מכוסה), `undefined` = fallback ל-`headlineTrait`. גוצ'ה שנתפסה: אם עושים `(traits ?? [])[0] ?? headlineTrait(p)` אז מערך ריק נופל ל-fallback וכל שחקן מקבל אופי, מה שמבטל את שער ה-65% וגם מחזיר כפילויות. חייב `traits ? (traits[0] ?? null) : headlineTrait(p)`.

**חדר ההלבשה** בהיכרות הראשונה עם הסגל: ארבעה שחקנים **שיש להם אופי** מציגים את עצמם, לחיצה פותחת כרטיס.

**השפעת תכונות על המשחק** (`game/traitEffects.ts`, בשימוש **רק ב-`liveMatch.ts`**, לבקשת איציק "לא גורם משמעותי"): השפעות זעירות בלבד. `playerMods(traits)` → `fitnessStart` (מנוע/ברזל/חומוס +3, חיית לילה -4, אבא חדש/מילואים -3), `fatigueMul` (מנוע 0.90, לא אוהב לרוץ 1.12, עבודה ביום 1.05), `cardProne` (חם מזג 3, צולל/מדבר עם השופט 2). `teamMoraleBump` מוגבל ל-+4 (מנהיג/ליצן/דוד החדר). ב-`createLive`: `applySquadTraits` מריץ `assignTraits` על כל צד, מחיל fitness+morale, ושומר `mods: Map<id, PlayerMods>` ב-LiveState. `applyFatigue` מכפיל את השחיקה ב-`fatigueMul`. `maybeDiscipline` **לא משנה את קצב הכרטיסים**, רק בוחר את מקבל הכרטיס במשקל `cardProne`. **קריטי: matchEngine המכויל (שאר משחקי המחזור) לא נגע.** אימות headless (400 משחקים חיים): שערים/משחק 2.67 (זהה ל-2.7 המכויל), cardProne ממוצע של מקבל כרטיס 1.27 מול בסיס 1.14, כלומר נטייה עדינה ולא מכריעה.

**הכרוניקה שלך** (`chronicle.ts` + `Chronicle.tsx`): אוטוביוגרפיה שנכתבת לבד. אחרי כל מחזור `commitRound` מריץ `chronicleAfterRound(prev, next, result)` ומוסיף רגעים שנתפסו: ניצחון ראשון, הפסד ראשון, ניצחון דרבי, הפסד דרבי, קאמבק (היינו מאחור וניצחנו), הצגה בהפרש 3+, עלייה לראש הטבלה, שבירת רצף הפסדים של 3+, וכישרון עד גיל 20 שכבש שער בכורה. בסוף עונה נוסף כרטיס סיכום ב-`advancePastPress`. כניסה מה-Hub עם באדג' זהב לפרקים שלא נקראו (`chronicleSeen`). קיבולת: כל entry עם `id` יציב, אז אין כפילויות.

**שמירה אוטומטית** לקריירה, מסך פתיחה מציע "המשך" או "קריירה חדשה". `loadCareer` ממלא שדות חדשים שחסרים בסייב ישן (`chronicle`, `chronicleSeen`), כך שסייב ישן לא קורס.

**עיצוב "פוסטר יציע"** (הוחלף מ"אור זרקורים" הגנרי לפי בקשת איציק): רקע כהה עם זרקורים, פסי כיסוח דשא עדינים, גרעין פילם, ויניאטה. **Karantina** (700) לכותרות ולכל המספרים הגדולים, Heebo לגוף. חתימת צורה: "פלטת שידור", פינה אחת חדה בכרטיסים (`--plate`), חיתוך משופע (chamfer clip-path) בכפתור הראשי ובסגמנט פעיל, eyebrow הפך לתגית זהב מוטה (skew). מחלקת `.score-face` למספרי לוח תוצאות. אפס אימוג'י כאייקונים, הכל SVG.

**RTL תוקן** בכל המקומות שהיה חשוב: `ScorePair` שומר שספרת הבית ליד סמל הבית, כפתורי הבעיטה `direction: ltr` כדי שהכיוון יתאים לרמז השוער.

---

## פרטים טכניים שחשוב לזכור (אחרת חוזרים לבאגים)

- **פופ-אפים חייבים Portal:** ל-`.screen` יש `transform` מאנימציית הכניסה, וזה יוצר containing block ששובר `position:fixed` של כל overlay שנמצא בתוכו, אז במסך גלול הכרטיס נפתח בראש הדף במקום איפה שאתה. הפתרון: `components/Portal.tsx` (createPortal ל-body). כל overlay עם position:fixed חייב להיות עטוף ב-`<Portal>`: PlayerCard, MomentPopup (Match), ContractSheet (Transfers). אם מוסיפים overlay חדש, לעטוף אותו גם.
- **גופן:** התצוגה היא **Rubik** (800/900), לא Karantina (שנפסל כ"מיושן ולא ברור" בנייד). Rubik רחב יותר מ-condensed, אז גדלי התצוגה ירדו (h1 clamp 30-40, h2 22, btn 17, meter 19) וה-letter-spacing עבר לאפס/שלילי קטן (עברית לא אוהבת tracking חיובי). `.score-face` weight 800 tabular. Heebo נשאר לגוף. אם משנים גופן תצוגה, לבדוק overflow ב-375px.
- **StrictMode + interval:** אסור לעשות side-effects בתוך setState updater. הלוגיקה של `Match.tsx` פועלת רק על הדקה, כל השאר derived. אם משנים, לוודא שאירועים לא מוכפלים.
- **Vite HMR רועש:** אחרי שינויים גדולים לפעמים הקונסול מציג `ReferenceError: X is not defined` משאריות. זה לא באג אמיתי, ריענון קשה או `rm -rf node_modules/.vite` פותר.
- **Windows Bash gotcha:** `python3` פה הוא stub של Windows Store, לא לסמוך עליו. יש `ffmpeg` דרך WinGet, בשימוש לדחיסת התמונות ל-webp.
- **`localhost:5180` הוא הפורט הקבוע.** אם התפוס, Vite קופץ הלאה.
- **Chrome PDF:** רינדור עברי RTL עובד רק בפלט מ-headless Chrome, לא מ-WeasyPrint. `chrome.exe --headless --print-to-pdf`.
- **מנוע מכויל:** 10,000 סימולציות נותנות 2.7 שערים/משחק, 46% ניצחון בית, 26% תיקו, 6.6% אפס אפס, +8 OVR = 69% ניצחון. אל תגעו במקדמים בלי `sanityCheck`.
- **שקיפות בצבעי טוקנים:** אסור לכתוב `` `${color}55` `` כשה-color הוא `var(--gold)`, זה מייצר CSS לא חוקי והדפדפן נופל ל-`currentColor`. להשתמש ב-`color-mix(in srgb, var(--gold) 34%, transparent)`. (`ActionCard` ב-Hub עדיין עם התבנית הישנה, לתקן כשנוגעים בו.)
- **chamfer + צל/פוקוס:** `clip-path` חותך גם box-shadow וגם outline. צל לכפתור הראשי נעשה עם `filter: drop-shadow(...)` (עוקב אחרי הצורה), ופוקוס עם `box-shadow: inset` (נשאר בתוך החיתוך).
- **אופי שחקנים, מלכודת פיזור:** רק תכונות עם `signature: true` מובילות. אם מסמנים גם `fit: outfield` (מסנן בלבד) כמובילה, כל שחקני השדה נשאבים מבריכה קטנה וחצי הסגל מקבל "לא אוהב לרוץ". נבדק: התכונה השכיחה ביותר סביב 5%, לא 50%.
- **אופי, סמכות המערך:** ב-PlayerRow `traits` prop ריק חייב להישאר ריק, לא ליפול ל-fallback. אחרת שער ה-65% וה-dedup נשברים. סקריפט אימות: לייבא personalities+matchEngine ב-node עם file:/// URL, לבנות סגל של 16 עם OVR מגוון, ולוודא כיסוי ~63%, 0 כפילויות, 0 חלשים(<52) עם אופי.
- **Karantina מעודן:** פונט condensed, אז גדלים רצים גדולים (h1 עד 50px, h2 27px) ו-letter-spacing חיובי קטן, לעולם לא שלילי. משקלים זמינים: 300/400/700 בלבד.

---

## Higgsfield credits

יתרה: **~903 קרדיטים** (מתוך 911 בתחילת השיחה). **מודל בשימוש:** `nano_banana_pro`, 2K, 9:16, 2 קרדיטים לתמונה. `models_explore` פועל, `generate_image_batch` + `jobs_wait` הזרימה הסטנדרטית.

---

## המשימות הבאות שאיציק הזכיר או שנשארו פתוחות

**פידבק אחרון של איציק (נשאר לפני שהתחלנו בפופ אפ):**
> "המשחק עצמו לא ייגרום לי לחזור אליו כרגע. הוא נראה מאוד בסיסי. בוא נחשוב ביחד אחרי שעשינו את כל השינויים איך משפרים אותו שייגרום לחזור עוד פעם"

**ההמלצה שלי לצעד הבא**, בסדר החזר ההשקעה:

1. **המחזור המשותף** בשעה קבועה. כל יום ב-21:00 כל השחקנים בליגה מסמלצים ביחד, סוגרים הרכב לפני. זה יוצר appointment. הפיצ'ר הכי חשוב שלא נבנה.
2. **סאונד**: יציע נוהם, שריקה, ה"אוווו". זול ומשנה תחושה יותר מהכל.
3. **תמונת שיתוף אוטומטית** לסטורי אחרי רגעים גדולים (עלייה ליגה, ניצחון בדקה 90).
4. **יעד בעלים** בתחילת עונה + לחץ שמצטבר + פיטורים אמיתיים ("תסיים בשמינייה או שאתה בחוץ"). הופך עונה לסיפור עם משהו להפסיד.
5. **אימון ופיתוח שחקנים** לאורך העונה. משהו שמצטבר.
6. ~~**הכרוניקה שלך**~~ נבנה. הרחבות אפשריות: רגעים מהעברות (החתימה הכי יקרה), שיא אישי של חלוץ, וכפתור שיתוף מהכרטיס.

**רעיונות ישנים שהוצעו ולא נבנו:**
- אווטאר מאמן (שוליים אחרי סעיפים 1-5)
- קרן ובעיטה חופשית כרגעים נוספים בפופ אפ
- Base44 sync (המנוע רץ בקליינט, אפשר להעביר סימולציה לסרבר לאימות ואנטי צ'יט)

---

## איך פותחים שיחה חדשה

הודעה ראשונה מומלצת:

> ממשיכים את BE A PRO. הפרויקט ב-`C:\Users\itzik\Desktop\CLAUDE\be-a-pro`, יש שם `HANDOFF.md` עם הסטטוס המלא. הרץ `npm run dev`. הצעד הבא [שם המשימה מהרשימה למעלה, או משהו חדש].

הסקיל `be-a-pro-skill` יטען מעצמו כשתדבר על המשחק, ויש בו את כל ההיסטוריה של ההחלטות הגדולות (סטאק, כיול מנוע, שפה עיצובית).
